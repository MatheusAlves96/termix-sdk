"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.snippets."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_list_contract(client, mock_http_client):
    """Generated from GET /snippets in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=["x"])
    result = client.snippets.list()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/snippets")
    assert result == ["x"]


def test_create_contract(client, mock_http_client):
    """Generated from POST /snippets in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.snippets.create(
        name="x", content="x", description="x", folder="x", order=1, hostFilter="x", isNote=True
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/snippets")
    assert sent.json == {
        "name": "x",
        "content": "x",
        "description": "x",
        "folder": "x",
        "order": 1,
        "hostFilter": "x",
        "isNote": True,
    }
    assert result is not None


def test_retrieve_contract(client, mock_http_client):
    """Generated from GET /snippets/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body="x")
    result = client.snippets.retrieve("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/snippets/x")
    assert result is not None


def test_update_contract(client, mock_http_client):
    """Generated from PUT /snippets/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.snippets.update(
        "x",
        name="x",
        content="x",
        description="x",
        folder="x",
        order=1.0,
        hostFilter="x",
        isNote=True,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/snippets/x")
    assert sent.json == {
        "name": "x",
        "content": "x",
        "description": "x",
        "folder": "x",
        "order": 1.0,
        "hostFilter": "x",
        "isNote": True,
    }
    assert result is not None


def test_delete_contract(client, mock_http_client):
    """Generated from DELETE /snippets/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.snippets.delete("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/snippets/x")
    assert result.to_dict() == {"success": True}


def test_list_folders_contract(client, mock_http_client):
    """Generated from GET /snippets/folders in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=[{}])
    result = client.snippets.list_folders()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/snippets/folders")
    assert result == [{}]


def test_create_folder_contract(client, mock_http_client):
    """Generated from POST /snippets/folders in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.snippets.create_folder(name="x", color="x", icon="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/snippets/folders")
    assert sent.json == {"name": "x", "color": "x", "icon": "x"}
    assert result is not None


def test_update_folder_metadata_contract(client, mock_http_client):
    """Generated from PUT /snippets/folders/{name}/metadata in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.snippets.update_folder_metadata("x", color="x", icon="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/snippets/folders/x/metadata")
    assert sent.json == {"color": "x", "icon": "x"}
    assert result is not None


def test_rename_folder_contract(client, mock_http_client):
    """Generated from PUT /snippets/folders/rename in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "oldName": "x", "newName": "x"}
    )
    result = client.snippets.rename_folder(oldName="x", newName="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/snippets/folders/rename")
    assert sent.json == {"oldName": "x", "newName": "x"}
    assert result.to_dict() == {"success": True, "oldName": "x", "newName": "x"}


def test_delete_folder_contract(client, mock_http_client):
    """Generated from DELETE /snippets/folders/{name} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.snippets.delete_folder("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/snippets/folders/x")
    assert result.to_dict() == {"success": True}


def test_reorder_contract(client, mock_http_client):
    """Generated from PUT /snippets/reorder in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True, "updated": 1.0})
    result = client.snippets.reorder(snippets=[{"id": 1.0, "order": 1.0, "folder": "x"}])
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/snippets/reorder")
    assert sent.json == {"snippets": [{"id": 1.0, "order": 1.0, "folder": "x"}]}
    assert result.to_dict() == {"success": True, "updated": 1.0}


def test_execute_contract(client, mock_http_client):
    """Generated from POST /snippets/execute in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "output": "x", "error": "x"}
    )
    result = client.snippets.execute(snippetId=1, hostId=1, inputValues={})
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/snippets/execute")
    assert sent.json == {"snippetId": 1, "hostId": 1, "inputValues": {}}
    assert result.to_dict() == {"success": True, "output": "x", "error": "x"}


def test_export_contract(client, mock_http_client):
    """Generated from GET /snippets/export in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "snippets": [
                {
                    "name": "x",
                    "content": "x",
                    "description": "x",
                    "folder": "x",
                    "order": 1.0,
                    "hostFilter": "x",
                }
            ],
            "folders": [{"name": "x", "color": "x", "icon": "x"}],
        },
    )
    result = client.snippets.export()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/snippets/export")
    assert result.to_dict() == {
        "snippets": [
            {
                "name": "x",
                "content": "x",
                "description": "x",
                "folder": "x",
                "order": 1.0,
                "hostFilter": "x",
            }
        ],
        "folders": [{"name": "x", "color": "x", "icon": "x"}],
    }


def test_bulk_import_contract(client, mock_http_client):
    """Generated from POST /snippets/bulk-import in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "snippetsImported": 1.0,
            "snippetsSkipped": 1.0,
            "snippetsUpdated": 1.0,
            "foldersImported": 1.0,
            "foldersSkipped": 1.0,
            "failed": 1.0,
            "errors": ["x"],
        },
    )
    result = client.snippets.bulk_import(
        snippets=[
            {
                "name": "x",
                "content": "x",
                "description": "x",
                "folder": "x",
                "order": 1.0,
                "hostFilter": "x",
            }
        ],
        folders=[{"name": "x", "color": "x", "icon": "x"}],
        overwrite=True,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/snippets/bulk-import")
    assert sent.json == {
        "snippets": [
            {
                "name": "x",
                "content": "x",
                "description": "x",
                "folder": "x",
                "order": 1.0,
                "hostFilter": "x",
            }
        ],
        "folders": [{"name": "x", "color": "x", "icon": "x"}],
        "overwrite": True,
    }
    assert result.to_dict() == {
        "success": True,
        "snippetsImported": 1.0,
        "snippetsSkipped": 1.0,
        "snippetsUpdated": 1.0,
        "foldersImported": 1.0,
        "foldersSkipped": 1.0,
        "failed": 1.0,
        "errors": ["x"],
    }


@pytest.mark.asyncio
async def test_async_list_contract(async_client, mock_async_http_client):
    """Generated from GET /snippets in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=["x"])
    result = await async_client.snippets.list()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/snippets")
    assert result == ["x"]


@pytest.mark.asyncio
async def test_async_create_contract(async_client, mock_async_http_client):
    """Generated from POST /snippets in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.snippets.create(
        name="x", content="x", description="x", folder="x", order=1, hostFilter="x", isNote=True
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/snippets")
    assert sent.json == {
        "name": "x",
        "content": "x",
        "description": "x",
        "folder": "x",
        "order": 1,
        "hostFilter": "x",
        "isNote": True,
    }
    assert result is not None


@pytest.mark.asyncio
async def test_async_retrieve_contract(async_client, mock_async_http_client):
    """Generated from GET /snippets/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body="x")
    result = await async_client.snippets.retrieve("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/snippets/x")
    assert result is not None


@pytest.mark.asyncio
async def test_async_update_contract(async_client, mock_async_http_client):
    """Generated from PUT /snippets/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.snippets.update(
        "x",
        name="x",
        content="x",
        description="x",
        folder="x",
        order=1.0,
        hostFilter="x",
        isNote=True,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/snippets/x")
    assert sent.json == {
        "name": "x",
        "content": "x",
        "description": "x",
        "folder": "x",
        "order": 1.0,
        "hostFilter": "x",
        "isNote": True,
    }
    assert result is not None


@pytest.mark.asyncio
async def test_async_delete_contract(async_client, mock_async_http_client):
    """Generated from DELETE /snippets/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.snippets.delete("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/snippets/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_list_folders_contract(async_client, mock_async_http_client):
    """Generated from GET /snippets/folders in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=[{}])
    result = await async_client.snippets.list_folders()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/snippets/folders")
    assert result == [{}]


@pytest.mark.asyncio
async def test_async_create_folder_contract(async_client, mock_async_http_client):
    """Generated from POST /snippets/folders in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.snippets.create_folder(name="x", color="x", icon="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/snippets/folders")
    assert sent.json == {"name": "x", "color": "x", "icon": "x"}
    assert result is not None


@pytest.mark.asyncio
async def test_async_update_folder_metadata_contract(async_client, mock_async_http_client):
    """Generated from PUT /snippets/folders/{name}/metadata in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.snippets.update_folder_metadata("x", color="x", icon="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/snippets/folders/x/metadata")
    assert sent.json == {"color": "x", "icon": "x"}
    assert result is not None


@pytest.mark.asyncio
async def test_async_rename_folder_contract(async_client, mock_async_http_client):
    """Generated from PUT /snippets/folders/rename in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "oldName": "x", "newName": "x"}
    )
    result = await async_client.snippets.rename_folder(oldName="x", newName="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/snippets/folders/rename")
    assert sent.json == {"oldName": "x", "newName": "x"}
    assert result.to_dict() == {"success": True, "oldName": "x", "newName": "x"}


@pytest.mark.asyncio
async def test_async_delete_folder_contract(async_client, mock_async_http_client):
    """Generated from DELETE /snippets/folders/{name} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.snippets.delete_folder("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/snippets/folders/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_reorder_contract(async_client, mock_async_http_client):
    """Generated from PUT /snippets/reorder in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True, "updated": 1.0})
    result = await async_client.snippets.reorder(
        snippets=[{"id": 1.0, "order": 1.0, "folder": "x"}]
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/snippets/reorder")
    assert sent.json == {"snippets": [{"id": 1.0, "order": 1.0, "folder": "x"}]}
    assert result.to_dict() == {"success": True, "updated": 1.0}


@pytest.mark.asyncio
async def test_async_execute_contract(async_client, mock_async_http_client):
    """Generated from POST /snippets/execute in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "output": "x", "error": "x"}
    )
    result = await async_client.snippets.execute(snippetId=1, hostId=1, inputValues={})
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/snippets/execute")
    assert sent.json == {"snippetId": 1, "hostId": 1, "inputValues": {}}
    assert result.to_dict() == {"success": True, "output": "x", "error": "x"}


@pytest.mark.asyncio
async def test_async_export_contract(async_client, mock_async_http_client):
    """Generated from GET /snippets/export in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "snippets": [
                {
                    "name": "x",
                    "content": "x",
                    "description": "x",
                    "folder": "x",
                    "order": 1.0,
                    "hostFilter": "x",
                }
            ],
            "folders": [{"name": "x", "color": "x", "icon": "x"}],
        },
    )
    result = await async_client.snippets.export()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/snippets/export")
    assert result.to_dict() == {
        "snippets": [
            {
                "name": "x",
                "content": "x",
                "description": "x",
                "folder": "x",
                "order": 1.0,
                "hostFilter": "x",
            }
        ],
        "folders": [{"name": "x", "color": "x", "icon": "x"}],
    }


@pytest.mark.asyncio
async def test_async_bulk_import_contract(async_client, mock_async_http_client):
    """Generated from POST /snippets/bulk-import in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "snippetsImported": 1.0,
            "snippetsSkipped": 1.0,
            "snippetsUpdated": 1.0,
            "foldersImported": 1.0,
            "foldersSkipped": 1.0,
            "failed": 1.0,
            "errors": ["x"],
        },
    )
    result = await async_client.snippets.bulk_import(
        snippets=[
            {
                "name": "x",
                "content": "x",
                "description": "x",
                "folder": "x",
                "order": 1.0,
                "hostFilter": "x",
            }
        ],
        folders=[{"name": "x", "color": "x", "icon": "x"}],
        overwrite=True,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/snippets/bulk-import")
    assert sent.json == {
        "snippets": [
            {
                "name": "x",
                "content": "x",
                "description": "x",
                "folder": "x",
                "order": 1.0,
                "hostFilter": "x",
            }
        ],
        "folders": [{"name": "x", "color": "x", "icon": "x"}],
        "overwrite": True,
    }
    assert result.to_dict() == {
        "success": True,
        "snippetsImported": 1.0,
        "snippetsSkipped": 1.0,
        "snippetsUpdated": 1.0,
        "foldersImported": 1.0,
        "foldersSkipped": 1.0,
        "failed": 1.0,
        "errors": ["x"],
    }
