"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.tunnel."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_get_status_by_name_contract(client, mock_http_client):
    """Generated from GET /ssh/tunnel/status/{tunnelName} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "name": "x",
            "status": {
                "connected": True,
                "status": "failed",
                "retryCount": 1.0,
                "maxRetries": 1.0,
                "nextRetryIn": 1.0,
                "reason": "x",
                "errorType": "CONNECTION_FAILED",
                "manualDisconnect": True,
                "retryExhausted": True,
                "connectionLogs": [{}],
            },
        },
    )
    result = client.tunnel.get_status_by_name("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/ssh/tunnel/status/x")
    assert result.to_dict() == {
        "name": "x",
        "status": {
            "connected": True,
            "status": "failed",
            "retryCount": 1.0,
            "maxRetries": 1.0,
            "nextRetryIn": 1.0,
            "reason": "x",
            "errorType": "CONNECTION_FAILED",
            "manualDisconnect": True,
            "retryExhausted": True,
            "connectionLogs": [{}],
        },
    }


def test_connect_contract(client, mock_http_client):
    """Generated from POST /ssh/tunnel/connect in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"message": "Connection request received", "tunnelName": "x"}
    )
    result = client.tunnel.connect(
        name="x",
        requestingUserId="x",
        sourceHostId=1.0,
        tunnelIndex=1.0,
        endpointIP="x",
        endpointUsername="x",
        endpointHost="x",
        endpointSSHPort=1.0,
        endpointAuthMethod="x",
        endpointKeyType="x",
        endpointCredentialId=1.0,
        endpointUserId="x",
        endpointPassword="x",
        endpointSSHKey="x",
        endpointKeyPassword="x",
        endpointPort=1.0,
        sourcePort=1.0,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/ssh/tunnel/connect")
    assert sent.json == {
        "name": "x",
        "requestingUserId": "x",
        "sourceHostId": 1.0,
        "tunnelIndex": 1.0,
        "endpointIP": "x",
        "endpointUsername": "x",
        "endpointHost": "x",
        "endpointSSHPort": 1.0,
        "endpointAuthMethod": "x",
        "endpointKeyType": "x",
        "endpointCredentialId": 1.0,
        "endpointUserId": "x",
        "endpointPassword": "x",
        "endpointSSHKey": "x",
        "endpointKeyPassword": "x",
        "endpointPort": 1.0,
        "sourcePort": 1.0,
    }
    assert result.to_dict() == {"message": "Connection request received", "tunnelName": "x"}


def test_disconnect_contract(client, mock_http_client):
    """Generated from POST /ssh/tunnel/disconnect in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"message": "Disconnect request received", "tunnelName": "x"}
    )
    result = client.tunnel.disconnect(tunnelName="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/ssh/tunnel/disconnect")
    assert sent.json == {"tunnelName": "x"}
    assert result.to_dict() == {"message": "Disconnect request received", "tunnelName": "x"}


def test_get_status_contract(client, mock_http_client):
    """Generated from GET /ssh/tunnel/status in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.tunnel.get_status()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/ssh/tunnel/status")
    assert result is not None


def test_cancel_contract(client, mock_http_client):
    """Generated from POST /ssh/tunnel/cancel in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"message": "Cancel request received", "tunnelName": "x"}
    )
    result = client.tunnel.cancel(tunnelName="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/ssh/tunnel/cancel")
    assert sent.json == {"tunnelName": "x"}
    assert result.to_dict() == {"message": "Cancel request received", "tunnelName": "x"}


@pytest.mark.asyncio
async def test_async_get_status_by_name_contract(async_client, mock_async_http_client):
    """Generated from GET /ssh/tunnel/status/{tunnelName} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "name": "x",
            "status": {
                "connected": True,
                "status": "failed",
                "retryCount": 1.0,
                "maxRetries": 1.0,
                "nextRetryIn": 1.0,
                "reason": "x",
                "errorType": "CONNECTION_FAILED",
                "manualDisconnect": True,
                "retryExhausted": True,
                "connectionLogs": [{}],
            },
        },
    )
    result = await async_client.tunnel.get_status_by_name("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/ssh/tunnel/status/x")
    assert result.to_dict() == {
        "name": "x",
        "status": {
            "connected": True,
            "status": "failed",
            "retryCount": 1.0,
            "maxRetries": 1.0,
            "nextRetryIn": 1.0,
            "reason": "x",
            "errorType": "CONNECTION_FAILED",
            "manualDisconnect": True,
            "retryExhausted": True,
            "connectionLogs": [{}],
        },
    }


@pytest.mark.asyncio
async def test_async_connect_contract(async_client, mock_async_http_client):
    """Generated from POST /ssh/tunnel/connect in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"message": "Connection request received", "tunnelName": "x"}
    )
    result = await async_client.tunnel.connect(
        name="x",
        requestingUserId="x",
        sourceHostId=1.0,
        tunnelIndex=1.0,
        endpointIP="x",
        endpointUsername="x",
        endpointHost="x",
        endpointSSHPort=1.0,
        endpointAuthMethod="x",
        endpointKeyType="x",
        endpointCredentialId=1.0,
        endpointUserId="x",
        endpointPassword="x",
        endpointSSHKey="x",
        endpointKeyPassword="x",
        endpointPort=1.0,
        sourcePort=1.0,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/ssh/tunnel/connect")
    assert sent.json == {
        "name": "x",
        "requestingUserId": "x",
        "sourceHostId": 1.0,
        "tunnelIndex": 1.0,
        "endpointIP": "x",
        "endpointUsername": "x",
        "endpointHost": "x",
        "endpointSSHPort": 1.0,
        "endpointAuthMethod": "x",
        "endpointKeyType": "x",
        "endpointCredentialId": 1.0,
        "endpointUserId": "x",
        "endpointPassword": "x",
        "endpointSSHKey": "x",
        "endpointKeyPassword": "x",
        "endpointPort": 1.0,
        "sourcePort": 1.0,
    }
    assert result.to_dict() == {"message": "Connection request received", "tunnelName": "x"}


@pytest.mark.asyncio
async def test_async_disconnect_contract(async_client, mock_async_http_client):
    """Generated from POST /ssh/tunnel/disconnect in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"message": "Disconnect request received", "tunnelName": "x"}
    )
    result = await async_client.tunnel.disconnect(tunnelName="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/ssh/tunnel/disconnect")
    assert sent.json == {"tunnelName": "x"}
    assert result.to_dict() == {"message": "Disconnect request received", "tunnelName": "x"}


@pytest.mark.asyncio
async def test_async_get_status_contract(async_client, mock_async_http_client):
    """Generated from GET /ssh/tunnel/status in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.tunnel.get_status()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/ssh/tunnel/status")
    assert result is not None


@pytest.mark.asyncio
async def test_async_cancel_contract(async_client, mock_async_http_client):
    """Generated from POST /ssh/tunnel/cancel in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"message": "Cancel request received", "tunnelName": "x"}
    )
    result = await async_client.tunnel.cancel(tunnelName="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/ssh/tunnel/cancel")
    assert sent.json == {"tunnelName": "x"}
    assert result.to_dict() == {"message": "Cancel request received", "tunnelName": "x"}
