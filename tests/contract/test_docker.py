"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.docker."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_get_container_stats_contract(client, mock_http_client):
    """Generated from GET /docker/containers/{sessionId}/{containerId}/stats in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "cpu": "x",
            "memoryUsed": "x",
            "memoryLimit": "x",
            "memoryPercent": "x",
            "netInput": "x",
            "netOutput": "x",
            "blockRead": "x",
            "blockWrite": "x",
            "pids": "x",
        },
    )
    result = client.docker.get_container_stats("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/containers/x/x/stats")
    assert result.to_dict() == {
        "cpu": "x",
        "memoryUsed": "x",
        "memoryLimit": "x",
        "memoryPercent": "x",
        "netInput": "x",
        "netOutput": "x",
        "blockRead": "x",
        "blockWrite": "x",
        "pids": "x",
    }


def test_get_container_contract(client, mock_http_client):
    """Generated from GET /docker/containers/{sessionId}/{containerId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "id": "x",
            "name": "x",
            "image": "x",
            "status": "x",
            "state": "created",
            "ports": "x",
            "created": "x",
            "command": "x",
            "labels": {},
            "networks": ["x"],
            "mounts": ["x"],
        },
    )
    result = client.docker.get_container("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/containers/x/x")
    assert result.to_dict() == {
        "id": "x",
        "name": "x",
        "image": "x",
        "status": "x",
        "state": "created",
        "ports": "x",
        "created": "x",
        "command": "x",
        "labels": {},
        "networks": ["x"],
        "mounts": ["x"],
    }


def test_start_contract(client, mock_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/start in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container started successfully"}
    )
    result = client.docker.start("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/start")
    assert result.to_dict() == {"success": True, "message": "Container started successfully"}


def test_stop_contract(client, mock_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/stop in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container stopped successfully"}
    )
    result = client.docker.stop("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/stop")
    assert result.to_dict() == {"success": True, "message": "Container stopped successfully"}


def test_restart_contract(client, mock_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/restart in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container restarted successfully"}
    )
    result = client.docker.restart("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/restart")
    assert result.to_dict() == {"success": True, "message": "Container restarted successfully"}


def test_pause_contract(client, mock_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/pause in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container paused successfully"}
    )
    result = client.docker.pause("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/pause")
    assert result.to_dict() == {"success": True, "message": "Container paused successfully"}


def test_unpause_contract(client, mock_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/unpause in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container unpaused successfully"}
    )
    result = client.docker.unpause("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/unpause")
    assert result.to_dict() == {"success": True, "message": "Container unpaused successfully"}


def test_remove_container_contract(client, mock_http_client):
    """Generated from DELETE /docker/containers/{sessionId}/{containerId}/remove in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container removed successfully"}
    )
    result = client.docker.remove_container("x", "x", force=True)
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/docker/containers/x/x/remove")
    assert sent.params == {"force": True}
    assert result.to_dict() == {"success": True, "message": "Container removed successfully"}


def test_list_containers_contract(client, mock_http_client):
    """Generated from GET /docker/containers/{sessionId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=["x"])
    result = client.docker.list_containers("x", all="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/containers/x")
    assert sent.params == {"all": "x"}
    assert result == ["x"]


def test_get_container_logs_contract(client, mock_http_client):
    """Generated from GET /docker/containers/{sessionId}/{containerId}/logs in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True, "logs": "x"})
    result = client.docker.get_container_logs(
        "x", "x", tail=1, timestamps=True, since="x", until="x"
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/containers/x/x/logs")
    assert sent.params == {"tail": 1, "timestamps": True, "since": "x", "until": "x"}
    assert result.to_dict() == {"success": True, "logs": "x"}


def test_disconnect_contract(client, mock_http_client):
    """Generated from POST /docker/ssh/disconnect in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "message": "SSH session disconnected"}
    )
    result = client.docker.disconnect(sessionId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/disconnect")
    assert sent.json == {"sessionId": "x"}
    assert result.to_dict() == {"success": True, "message": "SSH session disconnected"}


def test_connect_totp_contract(client, mock_http_client):
    """Generated from POST /docker/ssh/connect-totp in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={"status": "success", "message": "TOTP verified, SSH connection established"},
    )
    result = client.docker.connect_totp(sessionId="x", totpCode="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/connect-totp")
    assert sent.json == {"sessionId": "x", "totpCode": "x"}
    assert result.to_dict() == {
        "status": "success",
        "message": "TOTP verified, SSH connection established",
    }


def test_connect_warpgate_contract(client, mock_http_client):
    """Generated from POST /docker/ssh/connect-warpgate in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={"status": "success", "message": "Warpgate verified, SSH connection established"},
    )
    result = client.docker.connect_warpgate(sessionId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/connect-warpgate")
    assert sent.json == {"sessionId": "x"}
    assert result.to_dict() == {
        "status": "success",
        "message": "Warpgate verified, SSH connection established",
    }


def test_keepalive_contract(client, mock_http_client):
    """Generated from POST /docker/ssh/keepalive in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "connected": True,
            "message": "Session keepalive successful",
            "lastActive": 1.0,
        },
    )
    result = client.docker.keepalive(sessionId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/keepalive")
    assert sent.json == {"sessionId": "x"}
    assert result.to_dict() == {
        "success": True,
        "connected": True,
        "message": "Session keepalive successful",
        "lastActive": 1.0,
    }


def test_get_status_contract(client, mock_http_client):
    """Generated from GET /docker/ssh/status in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True, "connected": True})
    result = client.docker.get_status(sessionId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/ssh/status")
    assert sent.params == {"sessionId": "x"}
    assert result.to_dict() == {"success": True, "connected": True}


def test_validate_session_contract(client, mock_http_client):
    """Generated from GET /docker/validate/{sessionId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"available": True, "version": "x", "runtime": "docker"}
    )
    result = client.docker.validate_session("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/validate/x")
    assert result.to_dict() == {"available": True, "version": "x", "runtime": "docker"}


def test_connect_contract(client, mock_http_client):
    """Generated from POST /docker/ssh/connect in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "message": "SSH connection established",
            "connectionLogs": [{"type": "error", "details": "x", "stage": "error", "message": "x"}],
        },
    )
    result = client.docker.connect(
        sessionId="x",
        hostId=1.0,
        userProvidedPassword="x",
        userProvidedSshKey="x",
        userProvidedKeyPassword="x",
        useSocks5=True,
        socks5Host="x",
        socks5Port=1.0,
        socks5Username="x",
        socks5Password="x",
        socks5ProxyChain="x",
        forceKeyboardInteractive=True,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/connect")
    assert sent.json == {
        "sessionId": "x",
        "hostId": 1.0,
        "userProvidedPassword": "x",
        "userProvidedSshKey": "x",
        "userProvidedKeyPassword": "x",
        "useSocks5": True,
        "socks5Host": "x",
        "socks5Port": 1.0,
        "socks5Username": "x",
        "socks5Password": "x",
        "socks5ProxyChain": "x",
        "forceKeyboardInteractive": True,
    }
    assert result.to_dict() == {
        "success": True,
        "message": "SSH connection established",
        "connectionLogs": [{"type": "error", "details": "x", "stage": "error", "message": "x"}],
    }


@pytest.mark.asyncio
async def test_async_get_container_stats_contract(async_client, mock_async_http_client):
    """Generated from GET /docker/containers/{sessionId}/{containerId}/stats in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "cpu": "x",
            "memoryUsed": "x",
            "memoryLimit": "x",
            "memoryPercent": "x",
            "netInput": "x",
            "netOutput": "x",
            "blockRead": "x",
            "blockWrite": "x",
            "pids": "x",
        },
    )
    result = await async_client.docker.get_container_stats("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/containers/x/x/stats")
    assert result.to_dict() == {
        "cpu": "x",
        "memoryUsed": "x",
        "memoryLimit": "x",
        "memoryPercent": "x",
        "netInput": "x",
        "netOutput": "x",
        "blockRead": "x",
        "blockWrite": "x",
        "pids": "x",
    }


@pytest.mark.asyncio
async def test_async_get_container_contract(async_client, mock_async_http_client):
    """Generated from GET /docker/containers/{sessionId}/{containerId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "id": "x",
            "name": "x",
            "image": "x",
            "status": "x",
            "state": "created",
            "ports": "x",
            "created": "x",
            "command": "x",
            "labels": {},
            "networks": ["x"],
            "mounts": ["x"],
        },
    )
    result = await async_client.docker.get_container("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/containers/x/x")
    assert result.to_dict() == {
        "id": "x",
        "name": "x",
        "image": "x",
        "status": "x",
        "state": "created",
        "ports": "x",
        "created": "x",
        "command": "x",
        "labels": {},
        "networks": ["x"],
        "mounts": ["x"],
    }


@pytest.mark.asyncio
async def test_async_start_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/start in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container started successfully"}
    )
    result = await async_client.docker.start("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/start")
    assert result.to_dict() == {"success": True, "message": "Container started successfully"}


@pytest.mark.asyncio
async def test_async_stop_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/stop in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container stopped successfully"}
    )
    result = await async_client.docker.stop("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/stop")
    assert result.to_dict() == {"success": True, "message": "Container stopped successfully"}


@pytest.mark.asyncio
async def test_async_restart_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/restart in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container restarted successfully"}
    )
    result = await async_client.docker.restart("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/restart")
    assert result.to_dict() == {"success": True, "message": "Container restarted successfully"}


@pytest.mark.asyncio
async def test_async_pause_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/pause in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container paused successfully"}
    )
    result = await async_client.docker.pause("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/pause")
    assert result.to_dict() == {"success": True, "message": "Container paused successfully"}


@pytest.mark.asyncio
async def test_async_unpause_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/containers/{sessionId}/{containerId}/unpause in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container unpaused successfully"}
    )
    result = await async_client.docker.unpause("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/containers/x/x/unpause")
    assert result.to_dict() == {"success": True, "message": "Container unpaused successfully"}


@pytest.mark.asyncio
async def test_async_remove_container_contract(async_client, mock_async_http_client):
    """Generated from DELETE /docker/containers/{sessionId}/{containerId}/remove in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Container removed successfully"}
    )
    result = await async_client.docker.remove_container("x", "x", force=True)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/docker/containers/x/x/remove")
    assert sent.params == {"force": True}
    assert result.to_dict() == {"success": True, "message": "Container removed successfully"}


@pytest.mark.asyncio
async def test_async_list_containers_contract(async_client, mock_async_http_client):
    """Generated from GET /docker/containers/{sessionId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=["x"])
    result = await async_client.docker.list_containers("x", all="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/containers/x")
    assert sent.params == {"all": "x"}
    assert result == ["x"]


@pytest.mark.asyncio
async def test_async_get_container_logs_contract(async_client, mock_async_http_client):
    """Generated from GET /docker/containers/{sessionId}/{containerId}/logs in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True, "logs": "x"})
    result = await async_client.docker.get_container_logs(
        "x", "x", tail=1, timestamps=True, since="x", until="x"
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/containers/x/x/logs")
    assert sent.params == {"tail": 1, "timestamps": True, "since": "x", "until": "x"}
    assert result.to_dict() == {"success": True, "logs": "x"}


@pytest.mark.asyncio
async def test_async_disconnect_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/ssh/disconnect in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "message": "SSH session disconnected"}
    )
    result = await async_client.docker.disconnect(sessionId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/disconnect")
    assert sent.json == {"sessionId": "x"}
    assert result.to_dict() == {"success": True, "message": "SSH session disconnected"}


@pytest.mark.asyncio
async def test_async_connect_totp_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/ssh/connect-totp in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={"status": "success", "message": "TOTP verified, SSH connection established"},
    )
    result = await async_client.docker.connect_totp(sessionId="x", totpCode="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/connect-totp")
    assert sent.json == {"sessionId": "x", "totpCode": "x"}
    assert result.to_dict() == {
        "status": "success",
        "message": "TOTP verified, SSH connection established",
    }


@pytest.mark.asyncio
async def test_async_connect_warpgate_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/ssh/connect-warpgate in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={"status": "success", "message": "Warpgate verified, SSH connection established"},
    )
    result = await async_client.docker.connect_warpgate(sessionId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/connect-warpgate")
    assert sent.json == {"sessionId": "x"}
    assert result.to_dict() == {
        "status": "success",
        "message": "Warpgate verified, SSH connection established",
    }


@pytest.mark.asyncio
async def test_async_keepalive_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/ssh/keepalive in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "connected": True,
            "message": "Session keepalive successful",
            "lastActive": 1.0,
        },
    )
    result = await async_client.docker.keepalive(sessionId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/keepalive")
    assert sent.json == {"sessionId": "x"}
    assert result.to_dict() == {
        "success": True,
        "connected": True,
        "message": "Session keepalive successful",
        "lastActive": 1.0,
    }


@pytest.mark.asyncio
async def test_async_get_status_contract(async_client, mock_async_http_client):
    """Generated from GET /docker/ssh/status in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "connected": True}
    )
    result = await async_client.docker.get_status(sessionId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/ssh/status")
    assert sent.params == {"sessionId": "x"}
    assert result.to_dict() == {"success": True, "connected": True}


@pytest.mark.asyncio
async def test_async_validate_session_contract(async_client, mock_async_http_client):
    """Generated from GET /docker/validate/{sessionId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"available": True, "version": "x", "runtime": "docker"}
    )
    result = await async_client.docker.validate_session("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/docker/validate/x")
    assert result.to_dict() == {"available": True, "version": "x", "runtime": "docker"}


@pytest.mark.asyncio
async def test_async_connect_contract(async_client, mock_async_http_client):
    """Generated from POST /docker/ssh/connect in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "message": "SSH connection established",
            "connectionLogs": [{"type": "error", "details": "x", "stage": "error", "message": "x"}],
        },
    )
    result = await async_client.docker.connect(
        sessionId="x",
        hostId=1.0,
        userProvidedPassword="x",
        userProvidedSshKey="x",
        userProvidedKeyPassword="x",
        useSocks5=True,
        socks5Host="x",
        socks5Port=1.0,
        socks5Username="x",
        socks5Password="x",
        socks5ProxyChain="x",
        forceKeyboardInteractive=True,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/docker/ssh/connect")
    assert sent.json == {
        "sessionId": "x",
        "hostId": 1.0,
        "userProvidedPassword": "x",
        "userProvidedSshKey": "x",
        "userProvidedKeyPassword": "x",
        "useSocks5": True,
        "socks5Host": "x",
        "socks5Port": 1.0,
        "socks5Username": "x",
        "socks5Password": "x",
        "socks5ProxyChain": "x",
        "forceKeyboardInteractive": True,
    }
    assert result.to_dict() == {
        "success": True,
        "message": "SSH connection established",
        "connectionLogs": [{"type": "error", "details": "x", "stage": "error", "message": "x"}],
    }
