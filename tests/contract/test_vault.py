"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.vault."""

from __future__ import annotations

import pytest


def test_list_profiles_contract(client, mock_http_client):
    """Generated from GET /vault/profiles in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=["x"])
    result = client.vault.list_profiles()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/vault/profiles")
    assert result == ["x"]


def test_create_profile_contract(client, mock_http_client):
    """Generated from POST /vault/profiles in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.vault.create_profile(
        name="x",
        description="x",
        folder="x",
        tags=[],
        vaultAddr="x",
        vaultNamespace="x",
        oidcMount="x",
        oidcRole="x",
        sshMount="x",
        sshRole="x",
        validPrincipals="x",
        keyType="x",
        shared=True,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/vault/profiles")
    assert sent.json == {
        "name": "x",
        "description": "x",
        "folder": "x",
        "tags": [],
        "vaultAddr": "x",
        "vaultNamespace": "x",
        "oidcMount": "x",
        "oidcRole": "x",
        "sshMount": "x",
        "sshRole": "x",
        "validPrincipals": "x",
        "keyType": "x",
        "shared": True,
    }
    assert result is not None


def test_update_profile_contract(client, mock_http_client):
    """Generated from PUT /vault/profiles/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.vault.update_profile(
        "x",
        name="x",
        description="x",
        folder="x",
        tags="x",
        vaultAddr="x",
        vaultNamespace="x",
        oidcMount="x",
        oidcRole="x",
        sshMount="x",
        sshRole="x",
        validPrincipals="x",
        keyType="x",
        shared=True,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/vault/profiles/x")
    assert sent.json == {
        "name": "x",
        "description": "x",
        "folder": "x",
        "tags": "x",
        "vaultAddr": "x",
        "vaultNamespace": "x",
        "oidcMount": "x",
        "oidcRole": "x",
        "sshMount": "x",
        "sshRole": "x",
        "validPrincipals": "x",
        "keyType": "x",
        "shared": True,
    }
    assert result is not None


def test_delete_profile_contract(client, mock_http_client):
    """Generated from DELETE /vault/profiles/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.vault.delete_profile("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/vault/profiles/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_list_profiles_contract(async_client, mock_async_http_client):
    """Generated from GET /vault/profiles in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=["x"])
    result = await async_client.vault.list_profiles()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/vault/profiles")
    assert result == ["x"]


@pytest.mark.asyncio
async def test_async_create_profile_contract(async_client, mock_async_http_client):
    """Generated from POST /vault/profiles in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.vault.create_profile(
        name="x",
        description="x",
        folder="x",
        tags=[],
        vaultAddr="x",
        vaultNamespace="x",
        oidcMount="x",
        oidcRole="x",
        sshMount="x",
        sshRole="x",
        validPrincipals="x",
        keyType="x",
        shared=True,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/vault/profiles")
    assert sent.json == {
        "name": "x",
        "description": "x",
        "folder": "x",
        "tags": [],
        "vaultAddr": "x",
        "vaultNamespace": "x",
        "oidcMount": "x",
        "oidcRole": "x",
        "sshMount": "x",
        "sshRole": "x",
        "validPrincipals": "x",
        "keyType": "x",
        "shared": True,
    }
    assert result is not None


@pytest.mark.asyncio
async def test_async_update_profile_contract(async_client, mock_async_http_client):
    """Generated from PUT /vault/profiles/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.vault.update_profile(
        "x",
        name="x",
        description="x",
        folder="x",
        tags="x",
        vaultAddr="x",
        vaultNamespace="x",
        oidcMount="x",
        oidcRole="x",
        sshMount="x",
        sshRole="x",
        validPrincipals="x",
        keyType="x",
        shared=True,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/vault/profiles/x")
    assert sent.json == {
        "name": "x",
        "description": "x",
        "folder": "x",
        "tags": "x",
        "vaultAddr": "x",
        "vaultNamespace": "x",
        "oidcMount": "x",
        "oidcRole": "x",
        "sshMount": "x",
        "sshRole": "x",
        "validPrincipals": "x",
        "keyType": "x",
        "shared": True,
    }
    assert result is not None


@pytest.mark.asyncio
async def test_async_delete_profile_contract(async_client, mock_async_http_client):
    """Generated from DELETE /vault/profiles/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.vault.delete_profile("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/vault/profiles/x")
    assert result.to_dict() == {"success": True}
