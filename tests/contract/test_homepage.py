"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.homepage."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_get_favicon_contract(client, mock_http_client):
    """Generated from GET /homepage/favicon in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=b"binary-content-placeholder")
    result = client.homepage.get_favicon(url="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/favicon")
    assert sent.params == {"url": "x"}
    assert result.read() == b"binary-content-placeholder"


def test_update_item_contract(client, mock_http_client):
    """Generated from PUT /homepage/items/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.homepage.update_item(1, title="x", config={})
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/homepage/items/1")
    assert sent.json == {"title": "x", "config": {}}
    assert result is not None


def test_delete_item_contract(client, mock_http_client):
    """Generated from DELETE /homepage/items/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"message": "Homepage item deleted"})
    result = client.homepage.delete_item(1)
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/homepage/items/1")
    assert result.to_dict() == {"message": "Homepage item deleted"}


def test_list_items_contract(client, mock_http_client):
    """Generated from GET /homepage/items in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=[{}])
    result = client.homepage.list_items()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/items")
    assert result == [{}]


def test_create_item_contract(client, mock_http_client):
    """Generated from POST /homepage/items in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.homepage.create_item(typeId="x", title="x", config={})
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/homepage/items")
    assert sent.json == {"typeId": "x", "title": "x", "config": {}}
    assert result is not None


def test_get_layout_contract(client, mock_http_client):
    """Generated from GET /homepage/layout in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body="x")
    result = client.homepage.get_layout()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/layout")
    assert result is not None


def test_set_layout_contract(client, mock_http_client):
    """Generated from PUT /homepage/layout in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"layout": "x"})
    result = client.homepage.set_layout(
        entries=[{"itemId": 1.0, "x": 1.0, "y": 1.0, "w": 1.0, "h": 1.0, "zOrder": 1.0}],
        pan={"x": 1.0, "y": 1.0},
        zoom=1.0,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/homepage/layout")
    assert sent.json == {
        "entries": [{"itemId": 1.0, "x": 1.0, "y": 1.0, "w": 1.0, "h": 1.0, "zOrder": 1.0}],
        "pan": {"x": 1.0, "y": 1.0},
        "zoom": 1.0,
    }
    assert result.to_dict() == {"layout": "x"}


def test_ping_contract(client, mock_http_client):
    """Generated from GET /homepage/ping in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"ok": True, "statusCode": 1.0, "latencyMs": 1.0}
    )
    result = client.homepage.ping(url="x", ttl=1)
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/ping")
    assert sent.params == {"url": "x", "ttl": 1}
    assert result.to_dict() == {"ok": True, "statusCode": 1.0, "latencyMs": 1.0}


def test_proxy_contract(client, mock_http_client):
    """Generated from GET /homepage/proxy in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body="x")
    result = client.homepage.proxy(url="x", ttl=1)
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/proxy")
    assert sent.params == {"url": "x", "ttl": 1}
    assert result is not None


def test_rss_contract(client, mock_http_client):
    """Generated from GET /homepage/rss in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body=[{"title": "x", "link": "x", "pubDate": "x", "description": "x"}]
    )
    result = client.homepage.rss(url="x", max=1)
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/rss")
    assert sent.params == {"url": "x", "max": 1}
    assert len(result) == 1
    assert result[0].to_dict() == {"title": "x", "link": "x", "pubDate": "x", "description": "x"}


@pytest.mark.asyncio
async def test_async_get_favicon_contract(async_client, mock_async_http_client):
    """Generated from GET /homepage/favicon in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=b"binary-content-placeholder")
    result = await async_client.homepage.get_favicon(url="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/favicon")
    assert sent.params == {"url": "x"}
    assert await result.read() == b"binary-content-placeholder"


@pytest.mark.asyncio
async def test_async_update_item_contract(async_client, mock_async_http_client):
    """Generated from PUT /homepage/items/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.homepage.update_item(1, title="x", config={})
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/homepage/items/1")
    assert sent.json == {"title": "x", "config": {}}
    assert result is not None


@pytest.mark.asyncio
async def test_async_delete_item_contract(async_client, mock_async_http_client):
    """Generated from DELETE /homepage/items/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"message": "Homepage item deleted"}
    )
    result = await async_client.homepage.delete_item(1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/homepage/items/1")
    assert result.to_dict() == {"message": "Homepage item deleted"}


@pytest.mark.asyncio
async def test_async_list_items_contract(async_client, mock_async_http_client):
    """Generated from GET /homepage/items in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=[{}])
    result = await async_client.homepage.list_items()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/items")
    assert result == [{}]


@pytest.mark.asyncio
async def test_async_create_item_contract(async_client, mock_async_http_client):
    """Generated from POST /homepage/items in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.homepage.create_item(typeId="x", title="x", config={})
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/homepage/items")
    assert sent.json == {"typeId": "x", "title": "x", "config": {}}
    assert result is not None


@pytest.mark.asyncio
async def test_async_get_layout_contract(async_client, mock_async_http_client):
    """Generated from GET /homepage/layout in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body="x")
    result = await async_client.homepage.get_layout()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/layout")
    assert result is not None


@pytest.mark.asyncio
async def test_async_set_layout_contract(async_client, mock_async_http_client):
    """Generated from PUT /homepage/layout in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"layout": "x"})
    result = await async_client.homepage.set_layout(
        entries=[{"itemId": 1.0, "x": 1.0, "y": 1.0, "w": 1.0, "h": 1.0, "zOrder": 1.0}],
        pan={"x": 1.0, "y": 1.0},
        zoom=1.0,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/homepage/layout")
    assert sent.json == {
        "entries": [{"itemId": 1.0, "x": 1.0, "y": 1.0, "w": 1.0, "h": 1.0, "zOrder": 1.0}],
        "pan": {"x": 1.0, "y": 1.0},
        "zoom": 1.0,
    }
    assert result.to_dict() == {"layout": "x"}


@pytest.mark.asyncio
async def test_async_ping_contract(async_client, mock_async_http_client):
    """Generated from GET /homepage/ping in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"ok": True, "statusCode": 1.0, "latencyMs": 1.0}
    )
    result = await async_client.homepage.ping(url="x", ttl=1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/ping")
    assert sent.params == {"url": "x", "ttl": 1}
    assert result.to_dict() == {"ok": True, "statusCode": 1.0, "latencyMs": 1.0}


@pytest.mark.asyncio
async def test_async_proxy_contract(async_client, mock_async_http_client):
    """Generated from GET /homepage/proxy in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body="x")
    result = await async_client.homepage.proxy(url="x", ttl=1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/proxy")
    assert sent.params == {"url": "x", "ttl": 1}
    assert result is not None


@pytest.mark.asyncio
async def test_async_rss_contract(async_client, mock_async_http_client):
    """Generated from GET /homepage/rss in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body=[{"title": "x", "link": "x", "pubDate": "x", "description": "x"}]
    )
    result = await async_client.homepage.rss(url="x", max=1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/homepage/rss")
    assert sent.params == {"url": "x", "max": 1}
    assert len(result) == 1
    assert result[0].to_dict() == {"title": "x", "link": "x", "pubDate": "x", "description": "x"}
