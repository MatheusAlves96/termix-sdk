"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.proxmox."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_discover_contract(client, mock_http_client):
    """Generated from POST /proxmox/discover in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "guests": [
                {
                    "name": "x",
                    "vmid": 1.0,
                    "type": "qemu",
                    "node": "x",
                    "status": "x",
                    "ip": "x",
                    "connectionType": "ssh",
                    "enableDocker": True,
                }
            ],
            "credentialId": 1.0,
            "defaultCredentialId": 1.0,
            "jumpHosts": ["x"],
        },
    )
    result = client.proxmox.discover(hostId=1)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox/discover")
    assert sent.json == {"hostId": 1}
    assert result.to_dict() == {
        "guests": [
            {
                "name": "x",
                "vmid": 1.0,
                "type": "qemu",
                "node": "x",
                "status": "x",
                "ip": "x",
                "connectionType": "ssh",
                "enableDocker": True,
            }
        ],
        "credentialId": 1.0,
        "defaultCredentialId": 1.0,
        "jumpHosts": ["x"],
    }


def test_sync_contract(client, mock_http_client):
    """Generated from POST /proxmox/sync in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "created": 1.0,
            "updated": 1.0,
            "markedMissing": 1.0,
            "skipped": 1.0,
            "errors": ["x"],
        },
    )
    result = client.proxmox.sync(hostId=1)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox/sync")
    assert sent.json == {"hostId": 1}
    assert result.to_dict() == {
        "created": 1.0,
        "updated": 1.0,
        "markedMissing": 1.0,
        "skipped": 1.0,
        "errors": ["x"],
    }


def test_discover_stream_contract(client, mock_http_client):
    """Generated from GET /proxmox/discover/stream in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=b'event: message\ndata: {"ok": true}\n\n')
    result = client.proxmox.discover_stream()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/proxmox/discover/stream")
    events = list(result)
    assert events[0].event == "message"
    assert events[0].json() == {"ok": True}


@pytest.mark.asyncio
async def test_async_discover_contract(async_client, mock_async_http_client):
    """Generated from POST /proxmox/discover in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "guests": [
                {
                    "name": "x",
                    "vmid": 1.0,
                    "type": "qemu",
                    "node": "x",
                    "status": "x",
                    "ip": "x",
                    "connectionType": "ssh",
                    "enableDocker": True,
                }
            ],
            "credentialId": 1.0,
            "defaultCredentialId": 1.0,
            "jumpHosts": ["x"],
        },
    )
    result = await async_client.proxmox.discover(hostId=1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox/discover")
    assert sent.json == {"hostId": 1}
    assert result.to_dict() == {
        "guests": [
            {
                "name": "x",
                "vmid": 1.0,
                "type": "qemu",
                "node": "x",
                "status": "x",
                "ip": "x",
                "connectionType": "ssh",
                "enableDocker": True,
            }
        ],
        "credentialId": 1.0,
        "defaultCredentialId": 1.0,
        "jumpHosts": ["x"],
    }


@pytest.mark.asyncio
async def test_async_sync_contract(async_client, mock_async_http_client):
    """Generated from POST /proxmox/sync in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "created": 1.0,
            "updated": 1.0,
            "markedMissing": 1.0,
            "skipped": 1.0,
            "errors": ["x"],
        },
    )
    result = await async_client.proxmox.sync(hostId=1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox/sync")
    assert sent.json == {"hostId": 1}
    assert result.to_dict() == {
        "created": 1.0,
        "updated": 1.0,
        "markedMissing": 1.0,
        "skipped": 1.0,
        "errors": ["x"],
    }


@pytest.mark.asyncio
async def test_async_discover_stream_contract(async_client, mock_async_http_client):
    """Generated from GET /proxmox/discover/stream in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body=b'event: message\ndata: {"ok": true}\n\n'
    )
    result = await async_client.proxmox.discover_stream()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/proxmox/discover/stream")
    events = [e async for e in result]
    assert events[0].event == "message"
    assert events[0].json() == {"ok": True}
