"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.fleets."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_transfer_pull_contract(client, mock_http_client):
    """Generated from POST /fleets/{id}/transfer/pull in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=b"binary-content-placeholder")
    result = client.fleets.transfer_pull("x", remotePath="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/transfer/pull")
    assert sent.json == {"remotePath": "x"}
    assert result.read() == b"binary-content-placeholder"


def test_get_inventory_contract(client, mock_http_client):
    """Generated from GET /fleets/{id}/inventory in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body=[{"hostId": 1.0, "hostName": "x", "inventory": {}}]
    )
    result = client.fleets.get_inventory("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/fleets/x/inventory")
    assert len(result) == 1
    assert result[0].to_dict() == {"hostId": 1.0, "hostName": "x", "inventory": {}}


def test_refresh_inventory_contract(client, mock_http_client):
    """Generated from POST /fleets/{id}/inventory in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "results": [
                {"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}
            ]
        },
    )
    result = client.fleets.refresh_inventory("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/inventory")
    assert result.to_dict() == {
        "results": [{"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}]
    }


def test_packages_action_contract(client, mock_http_client):
    """Generated from POST /fleets/{id}/packages in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "results": [
                {"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}
            ]
        },
    )
    result = client.fleets.packages_action("x", action="install", package="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/packages")
    assert sent.json == {"action": "install", "package": "x"}
    assert result.to_dict() == {
        "results": [{"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}]
    }


def test_create_contract(client, mock_http_client):
    """Generated from POST /fleets in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"tagRules": "x"})
    result = client.fleets.create(name="x", description="x", color="x", icon="x", tagRules=[])
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets")
    assert sent.json == {"name": "x", "description": "x", "color": "x", "icon": "x", "tagRules": []}
    assert result.to_dict() == {"tagRules": "x"}


def test_list_contract(client, mock_http_client):
    """Generated from GET /fleets in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=[{"tagRules": "x", "memberCount": 1.0}])
    result = client.fleets.list()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/fleets")
    assert len(result) == 1
    assert result[0].to_dict() == {"tagRules": "x", "memberCount": 1.0}


def test_update_contract(client, mock_http_client):
    """Generated from PATCH /fleets/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"tagRules": "x"})
    result = client.fleets.update("x", name="x", description="x", color="x", icon="x", tagRules=[])
    sent = mock_http_client.requests[0]
    assert sent.method == "PATCH"
    assert sent.url.endswith("/fleets/x")
    assert sent.json == {"name": "x", "description": "x", "color": "x", "icon": "x", "tagRules": []}
    assert result.to_dict() == {"tagRules": "x"}


def test_delete_contract(client, mock_http_client):
    """Generated from DELETE /fleets/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.fleets.delete("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/fleets/x")
    assert result.to_dict() == {"success": True}


def test_list_members_contract(client, mock_http_client):
    """Generated from GET /fleets/{id}/members in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body=[
            {
                "id": 1.0,
                "name": "x",
                "ip": "x",
                "tags": ["x"],
                "static": True,
                "permissionLevel": "manage",
            }
        ],
    )
    result = client.fleets.list_members("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/fleets/x/members")
    assert len(result) == 1
    assert result[0].to_dict() == {
        "id": 1.0,
        "name": "x",
        "ip": "x",
        "tags": ["x"],
        "static": True,
        "permissionLevel": "manage",
    }


def test_add_member_contract(client, mock_http_client):
    """Generated from POST /fleets/{id}/members in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.fleets.add_member("x", hostId=1)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/members")
    assert sent.json == {"hostId": 1}
    assert result.to_dict() == {"success": True}


def test_remove_member_contract(client, mock_http_client):
    """Generated from DELETE /fleets/{id}/members/{hostId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.fleets.remove_member("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/fleets/x/members/x")
    assert result.to_dict() == {"success": True}


def test_share_contract(client, mock_http_client):
    """Generated from POST /fleets/{id}/share in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "permissionLevel": "manage",
            "expiresAt": "x",
            "hostsShared": 1.0,
            "hostsTotal": 1.0,
            "hostResults": [{"hostId": 1.0, "shared": True, "reason": "x"}],
        },
    )
    result = client.fleets.share("x", durationHours="x", permissionLevel="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/share")
    assert sent.json == {"durationHours": "x", "permissionLevel": "x"}
    assert result.to_dict() == {
        "success": True,
        "permissionLevel": "manage",
        "expiresAt": "x",
        "hostsShared": 1.0,
        "hostsTotal": 1.0,
        "hostResults": [{"hostId": 1.0, "shared": True, "reason": "x"}],
    }


def test_execute_contract(client, mock_http_client):
    """Generated from POST /fleets/{id}/execute in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "results": [
                {"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}
            ]
        },
    )
    result = client.fleets.execute("x", command="x", inputValues={})
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/execute")
    assert sent.json == {"command": "x", "inputValues": {}}
    assert result.to_dict() == {
        "results": [{"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}]
    }


def test_transfer_push_contract(client, mock_http_client):
    """Generated from POST /fleets/{id}/transfer/push in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "results": [
                {"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}
            ]
        },
    )
    result = client.fleets.transfer_push("x", file=b"file-content-placeholder", remotePath="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/transfer/push")
    assert sent.data == {"remotePath": "x"}
    assert sent.files == {"file": b"file-content-placeholder"}
    assert result.to_dict() == {
        "results": [{"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}]
    }


@pytest.mark.asyncio
async def test_async_transfer_pull_contract(async_client, mock_async_http_client):
    """Generated from POST /fleets/{id}/transfer/pull in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=b"binary-content-placeholder")
    result = await async_client.fleets.transfer_pull("x", remotePath="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/transfer/pull")
    assert sent.json == {"remotePath": "x"}
    assert await result.read() == b"binary-content-placeholder"


@pytest.mark.asyncio
async def test_async_get_inventory_contract(async_client, mock_async_http_client):
    """Generated from GET /fleets/{id}/inventory in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body=[{"hostId": 1.0, "hostName": "x", "inventory": {}}]
    )
    result = await async_client.fleets.get_inventory("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/fleets/x/inventory")
    assert len(result) == 1
    assert result[0].to_dict() == {"hostId": 1.0, "hostName": "x", "inventory": {}}


@pytest.mark.asyncio
async def test_async_refresh_inventory_contract(async_client, mock_async_http_client):
    """Generated from POST /fleets/{id}/inventory in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "results": [
                {"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}
            ]
        },
    )
    result = await async_client.fleets.refresh_inventory("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/inventory")
    assert result.to_dict() == {
        "results": [{"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}]
    }


@pytest.mark.asyncio
async def test_async_packages_action_contract(async_client, mock_async_http_client):
    """Generated from POST /fleets/{id}/packages in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "results": [
                {"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}
            ]
        },
    )
    result = await async_client.fleets.packages_action("x", action="install", package="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/packages")
    assert sent.json == {"action": "install", "package": "x"}
    assert result.to_dict() == {
        "results": [{"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}]
    }


@pytest.mark.asyncio
async def test_async_create_contract(async_client, mock_async_http_client):
    """Generated from POST /fleets in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"tagRules": "x"})
    result = await async_client.fleets.create(
        name="x", description="x", color="x", icon="x", tagRules=[]
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets")
    assert sent.json == {"name": "x", "description": "x", "color": "x", "icon": "x", "tagRules": []}
    assert result.to_dict() == {"tagRules": "x"}


@pytest.mark.asyncio
async def test_async_list_contract(async_client, mock_async_http_client):
    """Generated from GET /fleets in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body=[{"tagRules": "x", "memberCount": 1.0}]
    )
    result = await async_client.fleets.list()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/fleets")
    assert len(result) == 1
    assert result[0].to_dict() == {"tagRules": "x", "memberCount": 1.0}


@pytest.mark.asyncio
async def test_async_update_contract(async_client, mock_async_http_client):
    """Generated from PATCH /fleets/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"tagRules": "x"})
    result = await async_client.fleets.update(
        "x", name="x", description="x", color="x", icon="x", tagRules=[]
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PATCH"
    assert sent.url.endswith("/fleets/x")
    assert sent.json == {"name": "x", "description": "x", "color": "x", "icon": "x", "tagRules": []}
    assert result.to_dict() == {"tagRules": "x"}


@pytest.mark.asyncio
async def test_async_delete_contract(async_client, mock_async_http_client):
    """Generated from DELETE /fleets/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.fleets.delete("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/fleets/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_list_members_contract(async_client, mock_async_http_client):
    """Generated from GET /fleets/{id}/members in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body=[
            {
                "id": 1.0,
                "name": "x",
                "ip": "x",
                "tags": ["x"],
                "static": True,
                "permissionLevel": "manage",
            }
        ],
    )
    result = await async_client.fleets.list_members("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/fleets/x/members")
    assert len(result) == 1
    assert result[0].to_dict() == {
        "id": 1.0,
        "name": "x",
        "ip": "x",
        "tags": ["x"],
        "static": True,
        "permissionLevel": "manage",
    }


@pytest.mark.asyncio
async def test_async_add_member_contract(async_client, mock_async_http_client):
    """Generated from POST /fleets/{id}/members in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.fleets.add_member("x", hostId=1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/members")
    assert sent.json == {"hostId": 1}
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_remove_member_contract(async_client, mock_async_http_client):
    """Generated from DELETE /fleets/{id}/members/{hostId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.fleets.remove_member("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/fleets/x/members/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_share_contract(async_client, mock_async_http_client):
    """Generated from POST /fleets/{id}/share in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "permissionLevel": "manage",
            "expiresAt": "x",
            "hostsShared": 1.0,
            "hostsTotal": 1.0,
            "hostResults": [{"hostId": 1.0, "shared": True, "reason": "x"}],
        },
    )
    result = await async_client.fleets.share("x", durationHours="x", permissionLevel="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/share")
    assert sent.json == {"durationHours": "x", "permissionLevel": "x"}
    assert result.to_dict() == {
        "success": True,
        "permissionLevel": "manage",
        "expiresAt": "x",
        "hostsShared": 1.0,
        "hostsTotal": 1.0,
        "hostResults": [{"hostId": 1.0, "shared": True, "reason": "x"}],
    }


@pytest.mark.asyncio
async def test_async_execute_contract(async_client, mock_async_http_client):
    """Generated from POST /fleets/{id}/execute in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "results": [
                {"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}
            ]
        },
    )
    result = await async_client.fleets.execute("x", command="x", inputValues={})
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/execute")
    assert sent.json == {"command": "x", "inputValues": {}}
    assert result.to_dict() == {
        "results": [{"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}]
    }


@pytest.mark.asyncio
async def test_async_transfer_push_contract(async_client, mock_async_http_client):
    """Generated from POST /fleets/{id}/transfer/push in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "results": [
                {"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}
            ]
        },
    )
    result = await async_client.fleets.transfer_push(
        "x", file=b"file-content-placeholder", remotePath="x"
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/fleets/x/transfer/push")
    assert sent.data == {"remotePath": "x"}
    assert sent.files == {"file": b"file-content-placeholder"}
    assert result.to_dict() == {
        "results": [{"hostId": 1.0, "hostName": "x", "success": True, "output": "x", "error": "x"}]
    }
