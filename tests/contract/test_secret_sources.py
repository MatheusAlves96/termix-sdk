"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.secret_sources."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_list_contract(client, mock_http_client):
    """Generated from GET /secret-sources in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"sources": [{"owned": True}]})
    result = client.secret_sources.list()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/secret-sources")
    assert result.to_dict() == {"sources": [{"owned": True}]}


def test_create_contract(client, mock_http_client):
    """Generated from POST /secret-sources in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"source": {"owned": True}})
    result = client.secret_sources.create(name="x", kind="x", baseUrl="x", token="x", shared=True)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/secret-sources")
    assert sent.json == {"name": "x", "kind": "x", "baseUrl": "x", "token": "x", "shared": True}
    assert result.to_dict() == {"source": {"owned": True}}


def test_update_contract(client, mock_http_client):
    """Generated from PUT /secret-sources/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.secret_sources.update(
        "x", name="x", baseUrl="x", token="x", shared=True, kind="x"
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/secret-sources/x")
    assert sent.json == {"name": "x", "baseUrl": "x", "token": "x", "shared": True, "kind": "x"}
    assert result.to_dict() == {"success": True}


def test_delete_contract(client, mock_http_client):
    """Generated from DELETE /secret-sources/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.secret_sources.delete("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/secret-sources/x")
    assert result.to_dict() == {"success": True}


def test_test_contract(client, mock_http_client):
    """Generated from POST /secret-sources/{id}/test in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True, "vaults": 1.0})
    result = client.secret_sources.test("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/secret-sources/x/test")
    assert result.to_dict() == {"ok": True, "vaults": 1.0}


@pytest.mark.asyncio
async def test_async_list_contract(async_client, mock_async_http_client):
    """Generated from GET /secret-sources in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"sources": [{"owned": True}]})
    result = await async_client.secret_sources.list()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/secret-sources")
    assert result.to_dict() == {"sources": [{"owned": True}]}


@pytest.mark.asyncio
async def test_async_create_contract(async_client, mock_async_http_client):
    """Generated from POST /secret-sources in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"source": {"owned": True}})
    result = await async_client.secret_sources.create(
        name="x", kind="x", baseUrl="x", token="x", shared=True
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/secret-sources")
    assert sent.json == {"name": "x", "kind": "x", "baseUrl": "x", "token": "x", "shared": True}
    assert result.to_dict() == {"source": {"owned": True}}


@pytest.mark.asyncio
async def test_async_update_contract(async_client, mock_async_http_client):
    """Generated from PUT /secret-sources/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.secret_sources.update(
        "x", name="x", baseUrl="x", token="x", shared=True, kind="x"
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/secret-sources/x")
    assert sent.json == {"name": "x", "baseUrl": "x", "token": "x", "shared": True, "kind": "x"}
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_delete_contract(async_client, mock_async_http_client):
    """Generated from DELETE /secret-sources/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.secret_sources.delete("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/secret-sources/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_test_contract(async_client, mock_async_http_client):
    """Generated from POST /secret-sources/{id}/test in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True, "vaults": 1.0})
    result = await async_client.secret_sources.test("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/secret-sources/x/test")
    assert result.to_dict() == {"ok": True, "vaults": 1.0}
