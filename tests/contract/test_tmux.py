"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.tmux."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_get_overview_contract(client, mock_http_client):
    """Generated from GET /tmux_monitor/{hostId}/overview in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"available": True, "sessions": [{}]})
    result = client.tmux.get_overview("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/tmux_monitor/x/overview")
    assert result.to_dict() == {"available": True, "sessions": [{}]}


def test_focus_contract(client, mock_http_client):
    """Generated from POST /tmux_monitor/{hostId}/focus in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True})
    result = client.tmux.focus("x", paneId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/focus")
    assert sent.json == {"paneId": "x"}
    assert result.to_dict() == {"ok": True}


def test_list_sessions_contract(client, mock_http_client):
    """Generated from POST /tmux_monitor/{hostId}/sessions in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True, "name": "x"})
    result = client.tmux.list_sessions("x", name="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/sessions")
    assert sent.json == {"name": "x"}
    assert result.to_dict() == {"ok": True, "name": "x"}


def test_list_windows_contract(client, mock_http_client):
    """Generated from POST /tmux_monitor/{hostId}/windows in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True})
    result = client.tmux.list_windows("x", sessionName="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/windows")
    assert sent.json == {"sessionName": "x"}
    assert result.to_dict() == {"ok": True}


def test_rename_contract(client, mock_http_client):
    """Generated from POST /tmux_monitor/{hostId}/rename in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True, "name": "x"})
    result = client.tmux.rename("x", sessionName="x", newName="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/rename")
    assert sent.json == {"sessionName": "x", "newName": "x"}
    assert result.to_dict() == {"ok": True, "name": "x"}


def test_kill_contract(client, mock_http_client):
    """Generated from POST /tmux_monitor/{hostId}/kill in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True})
    result = client.tmux.kill("x", sessionName="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/kill")
    assert sent.json == {"sessionName": "x"}
    assert result.to_dict() == {"ok": True}


def test_kill_window_contract(client, mock_http_client):
    """Generated from POST /tmux_monitor/{hostId}/kill-window in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True})
    result = client.tmux.kill_window("x", sessionName="x", windowIndex=1.0)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/kill-window")
    assert sent.json == {"sessionName": "x", "windowIndex": 1.0}
    assert result.to_dict() == {"ok": True}


def test_kill_pane_contract(client, mock_http_client):
    """Generated from POST /tmux_monitor/{hostId}/kill-pane in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True})
    result = client.tmux.kill_pane("x", paneId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/kill-pane")
    assert sent.json == {"paneId": "x"}
    assert result.to_dict() == {"ok": True}


def test_split_contract(client, mock_http_client):
    """Generated from POST /tmux_monitor/{hostId}/split in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"ok": True})
    result = client.tmux.split("x", paneId="x", direction="v")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/split")
    assert sent.json == {"paneId": "x", "direction": "v"}
    assert result.to_dict() == {"ok": True}


def test_search_contract(client, mock_http_client):
    """Generated from GET /tmux_monitor/{hostId}/search in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "query": "x",
            "matches": [
                {"paneId": "x", "sessionName": "x", "windowIndex": 1.0, "line": 1.0, "text": "x"}
            ],
            "truncated": True,
            "searchedLines": 2000,
            "maxPanes": 100,
        },
    )
    result = client.tmux.search("x", q="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/tmux_monitor/x/search")
    assert sent.params == {"q": "x"}
    assert result.to_dict() == {
        "query": "x",
        "matches": [
            {"paneId": "x", "sessionName": "x", "windowIndex": 1.0, "line": 1.0, "text": "x"}
        ],
        "truncated": True,
        "searchedLines": 2000,
        "maxPanes": 100,
    }


def test_metrics_contract(client, mock_http_client):
    """Generated from GET /tmux_monitor/{hostId}/metrics in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"panes": [{}]})
    result = client.tmux.metrics("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/tmux_monitor/x/metrics")
    assert result.to_dict() == {"panes": [{}]}


def test_set_tags_contract(client, mock_http_client):
    """Generated from PUT /tmux_monitor/{hostId}/tags in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"sessionName": "x", "tags": ["x"]})
    result = client.tmux.set_tags("x", sessionName="x", tags=[])
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/tmux_monitor/x/tags")
    assert sent.json == {"sessionName": "x", "tags": []}
    assert result.to_dict() == {"sessionName": "x", "tags": ["x"]}


@pytest.mark.asyncio
async def test_async_get_overview_contract(async_client, mock_async_http_client):
    """Generated from GET /tmux_monitor/{hostId}/overview in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"available": True, "sessions": [{}]}
    )
    result = await async_client.tmux.get_overview("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/tmux_monitor/x/overview")
    assert result.to_dict() == {"available": True, "sessions": [{}]}


@pytest.mark.asyncio
async def test_async_focus_contract(async_client, mock_async_http_client):
    """Generated from POST /tmux_monitor/{hostId}/focus in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True})
    result = await async_client.tmux.focus("x", paneId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/focus")
    assert sent.json == {"paneId": "x"}
    assert result.to_dict() == {"ok": True}


@pytest.mark.asyncio
async def test_async_list_sessions_contract(async_client, mock_async_http_client):
    """Generated from POST /tmux_monitor/{hostId}/sessions in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True, "name": "x"})
    result = await async_client.tmux.list_sessions("x", name="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/sessions")
    assert sent.json == {"name": "x"}
    assert result.to_dict() == {"ok": True, "name": "x"}


@pytest.mark.asyncio
async def test_async_list_windows_contract(async_client, mock_async_http_client):
    """Generated from POST /tmux_monitor/{hostId}/windows in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True})
    result = await async_client.tmux.list_windows("x", sessionName="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/windows")
    assert sent.json == {"sessionName": "x"}
    assert result.to_dict() == {"ok": True}


@pytest.mark.asyncio
async def test_async_rename_contract(async_client, mock_async_http_client):
    """Generated from POST /tmux_monitor/{hostId}/rename in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True, "name": "x"})
    result = await async_client.tmux.rename("x", sessionName="x", newName="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/rename")
    assert sent.json == {"sessionName": "x", "newName": "x"}
    assert result.to_dict() == {"ok": True, "name": "x"}


@pytest.mark.asyncio
async def test_async_kill_contract(async_client, mock_async_http_client):
    """Generated from POST /tmux_monitor/{hostId}/kill in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True})
    result = await async_client.tmux.kill("x", sessionName="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/kill")
    assert sent.json == {"sessionName": "x"}
    assert result.to_dict() == {"ok": True}


@pytest.mark.asyncio
async def test_async_kill_window_contract(async_client, mock_async_http_client):
    """Generated from POST /tmux_monitor/{hostId}/kill-window in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True})
    result = await async_client.tmux.kill_window("x", sessionName="x", windowIndex=1.0)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/kill-window")
    assert sent.json == {"sessionName": "x", "windowIndex": 1.0}
    assert result.to_dict() == {"ok": True}


@pytest.mark.asyncio
async def test_async_kill_pane_contract(async_client, mock_async_http_client):
    """Generated from POST /tmux_monitor/{hostId}/kill-pane in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True})
    result = await async_client.tmux.kill_pane("x", paneId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/kill-pane")
    assert sent.json == {"paneId": "x"}
    assert result.to_dict() == {"ok": True}


@pytest.mark.asyncio
async def test_async_split_contract(async_client, mock_async_http_client):
    """Generated from POST /tmux_monitor/{hostId}/split in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"ok": True})
    result = await async_client.tmux.split("x", paneId="x", direction="v")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/tmux_monitor/x/split")
    assert sent.json == {"paneId": "x", "direction": "v"}
    assert result.to_dict() == {"ok": True}


@pytest.mark.asyncio
async def test_async_search_contract(async_client, mock_async_http_client):
    """Generated from GET /tmux_monitor/{hostId}/search in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "query": "x",
            "matches": [
                {"paneId": "x", "sessionName": "x", "windowIndex": 1.0, "line": 1.0, "text": "x"}
            ],
            "truncated": True,
            "searchedLines": 2000,
            "maxPanes": 100,
        },
    )
    result = await async_client.tmux.search("x", q="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/tmux_monitor/x/search")
    assert sent.params == {"q": "x"}
    assert result.to_dict() == {
        "query": "x",
        "matches": [
            {"paneId": "x", "sessionName": "x", "windowIndex": 1.0, "line": 1.0, "text": "x"}
        ],
        "truncated": True,
        "searchedLines": 2000,
        "maxPanes": 100,
    }


@pytest.mark.asyncio
async def test_async_metrics_contract(async_client, mock_async_http_client):
    """Generated from GET /tmux_monitor/{hostId}/metrics in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"panes": [{}]})
    result = await async_client.tmux.metrics("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/tmux_monitor/x/metrics")
    assert result.to_dict() == {"panes": [{}]}


@pytest.mark.asyncio
async def test_async_set_tags_contract(async_client, mock_async_http_client):
    """Generated from PUT /tmux_monitor/{hostId}/tags in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"sessionName": "x", "tags": ["x"]})
    result = await async_client.tmux.set_tags("x", sessionName="x", tags=[])
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/tmux_monitor/x/tags")
    assert sent.json == {"sessionName": "x", "tags": []}
    assert result.to_dict() == {"sessionName": "x", "tags": ["x"]}
