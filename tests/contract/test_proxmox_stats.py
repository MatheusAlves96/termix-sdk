"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.proxmox_stats."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_get_history_contract(client, mock_http_client):
    """Generated from GET /proxmox-stats/history/{hostId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "rows": [
                {
                    "ts": "x",
                    "cpu_percent": 1.0,
                    "mem_percent": 1.0,
                    "disk_percent": 1.0,
                    "net_rx_bytes": 1.0,
                    "net_tx_bytes": 1.0,
                }
            ],
            "fromTs": "x",
            "toTs": "x",
        },
    )
    result = client.proxmox_stats.get_history(1)
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/proxmox-stats/history/1")
    assert result.to_dict() == {
        "rows": [
            {
                "ts": "x",
                "cpu_percent": 1.0,
                "mem_percent": 1.0,
                "disk_percent": 1.0,
                "net_rx_bytes": 1.0,
                "net_tx_bytes": 1.0,
            }
        ],
        "fromTs": "x",
        "toTs": "x",
    }


def test_start_contract(client, mock_http_client):
    """Generated from POST /proxmox-stats/start/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "viewerSessionId": "x",
            "node": {"cpu": {}, "memory": {}, "disk": {}, "uptime": {}, "system": {}},
            "network": {"interfaces": [{}]},
            "guests": {"guests": [{}], "counts": {}},
            "storage": {"pools": [{}]},
            "cluster": "x",
            "lastChecked": "x",
        },
    )
    result = client.proxmox_stats.start(1)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/start/1")
    assert result.to_dict() == {
        "success": True,
        "viewerSessionId": "x",
        "node": {"cpu": {}, "memory": {}, "disk": {}, "uptime": {}, "system": {}},
        "network": {"interfaces": [{}]},
        "guests": {"guests": [{}], "counts": {}},
        "storage": {"pools": [{}]},
        "cluster": "x",
        "lastChecked": "x",
    }


def test_stop_contract(client, mock_http_client):
    """Generated from POST /proxmox-stats/stop/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.proxmox_stats.stop(1, viewerSessionId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/stop/1")
    assert sent.json == {"viewerSessionId": "x"}
    assert result.to_dict() == {"success": True}


def test_retrieve_contract(client, mock_http_client):
    """Generated from GET /proxmox-stats/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "node": {"cpu": {}, "memory": {}, "disk": {}, "uptime": {}, "system": {}},
            "network": {"interfaces": [{}]},
            "guests": {"guests": [{}], "counts": {}},
            "storage": {"pools": [{}]},
            "cluster": "x",
            "lastChecked": "x",
        },
    )
    result = client.proxmox_stats.retrieve(1)
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/proxmox-stats/1")
    assert result.to_dict() == {
        "node": {"cpu": {}, "memory": {}, "disk": {}, "uptime": {}, "system": {}},
        "network": {"interfaces": [{}]},
        "guests": {"guests": [{}], "counts": {}},
        "storage": {"pools": [{}]},
        "cluster": "x",
        "lastChecked": "x",
    }


def test_register_viewer_contract(client, mock_http_client):
    """Generated from POST /proxmox-stats/register-viewer in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": False, "skipped": True, "reason": "internal_error"}
    )
    result = client.proxmox_stats.register_viewer(hostId=1.0)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/register-viewer")
    assert sent.json == {"hostId": 1.0}
    assert result.to_dict() == {"success": False, "skipped": True, "reason": "internal_error"}


def test_unregister_viewer_contract(client, mock_http_client):
    """Generated from POST /proxmox-stats/unregister-viewer in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.proxmox_stats.unregister_viewer(hostId=1.0, viewerSessionId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/unregister-viewer")
    assert sent.json == {"hostId": 1.0, "viewerSessionId": "x"}
    assert result.to_dict() == {"success": True}


def test_heartbeat_contract(client, mock_http_client):
    """Generated from POST /proxmox-stats/heartbeat in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.proxmox_stats.heartbeat(viewerSessionId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/heartbeat")
    assert sent.json == {"viewerSessionId": "x"}
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_get_history_contract(async_client, mock_async_http_client):
    """Generated from GET /proxmox-stats/history/{hostId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "rows": [
                {
                    "ts": "x",
                    "cpu_percent": 1.0,
                    "mem_percent": 1.0,
                    "disk_percent": 1.0,
                    "net_rx_bytes": 1.0,
                    "net_tx_bytes": 1.0,
                }
            ],
            "fromTs": "x",
            "toTs": "x",
        },
    )
    result = await async_client.proxmox_stats.get_history(1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/proxmox-stats/history/1")
    assert result.to_dict() == {
        "rows": [
            {
                "ts": "x",
                "cpu_percent": 1.0,
                "mem_percent": 1.0,
                "disk_percent": 1.0,
                "net_rx_bytes": 1.0,
                "net_tx_bytes": 1.0,
            }
        ],
        "fromTs": "x",
        "toTs": "x",
    }


@pytest.mark.asyncio
async def test_async_start_contract(async_client, mock_async_http_client):
    """Generated from POST /proxmox-stats/start/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "viewerSessionId": "x",
            "node": {"cpu": {}, "memory": {}, "disk": {}, "uptime": {}, "system": {}},
            "network": {"interfaces": [{}]},
            "guests": {"guests": [{}], "counts": {}},
            "storage": {"pools": [{}]},
            "cluster": "x",
            "lastChecked": "x",
        },
    )
    result = await async_client.proxmox_stats.start(1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/start/1")
    assert result.to_dict() == {
        "success": True,
        "viewerSessionId": "x",
        "node": {"cpu": {}, "memory": {}, "disk": {}, "uptime": {}, "system": {}},
        "network": {"interfaces": [{}]},
        "guests": {"guests": [{}], "counts": {}},
        "storage": {"pools": [{}]},
        "cluster": "x",
        "lastChecked": "x",
    }


@pytest.mark.asyncio
async def test_async_stop_contract(async_client, mock_async_http_client):
    """Generated from POST /proxmox-stats/stop/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.proxmox_stats.stop(1, viewerSessionId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/stop/1")
    assert sent.json == {"viewerSessionId": "x"}
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_retrieve_contract(async_client, mock_async_http_client):
    """Generated from GET /proxmox-stats/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "node": {"cpu": {}, "memory": {}, "disk": {}, "uptime": {}, "system": {}},
            "network": {"interfaces": [{}]},
            "guests": {"guests": [{}], "counts": {}},
            "storage": {"pools": [{}]},
            "cluster": "x",
            "lastChecked": "x",
        },
    )
    result = await async_client.proxmox_stats.retrieve(1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/proxmox-stats/1")
    assert result.to_dict() == {
        "node": {"cpu": {}, "memory": {}, "disk": {}, "uptime": {}, "system": {}},
        "network": {"interfaces": [{}]},
        "guests": {"guests": [{}], "counts": {}},
        "storage": {"pools": [{}]},
        "cluster": "x",
        "lastChecked": "x",
    }


@pytest.mark.asyncio
async def test_async_register_viewer_contract(async_client, mock_async_http_client):
    """Generated from POST /proxmox-stats/register-viewer in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": False, "skipped": True, "reason": "internal_error"}
    )
    result = await async_client.proxmox_stats.register_viewer(hostId=1.0)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/register-viewer")
    assert sent.json == {"hostId": 1.0}
    assert result.to_dict() == {"success": False, "skipped": True, "reason": "internal_error"}


@pytest.mark.asyncio
async def test_async_unregister_viewer_contract(async_client, mock_async_http_client):
    """Generated from POST /proxmox-stats/unregister-viewer in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.proxmox_stats.unregister_viewer(hostId=1.0, viewerSessionId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/unregister-viewer")
    assert sent.json == {"hostId": 1.0, "viewerSessionId": "x"}
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_heartbeat_contract(async_client, mock_async_http_client):
    """Generated from POST /proxmox-stats/heartbeat in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.proxmox_stats.heartbeat(viewerSessionId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/proxmox-stats/heartbeat")
    assert sent.json == {"viewerSessionId": "x"}
    assert result.to_dict() == {"success": True}
