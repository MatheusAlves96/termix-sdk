"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.user_admin."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_make_admin_contract(client, mock_http_client):
    """Generated from POST /users/make-admin in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"message": "x"})
    result = client.user_admin.make_admin(userId="x", username="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/make-admin")
    assert sent.json == {"userId": "x", "username": "x"}
    assert result.to_dict() == {"message": "x"}


def test_remove_admin_contract(client, mock_http_client):
    """Generated from POST /users/remove-admin in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"message": "x"})
    result = client.user_admin.remove_admin(userId="x", username="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/remove-admin")
    assert sent.json == {"userId": "x", "username": "x"}
    assert result.to_dict() == {"message": "x"}


def test_create_user_contract(client, mock_http_client):
    """Generated from POST /users/admin-create in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={"message": "User created", "toast": {"type": "success", "message": "x"}},
    )
    result = client.user_admin.create_user(username="x", password="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/admin-create")
    assert sent.json == {"username": "x", "password": "x"}
    assert result.to_dict() == {
        "message": "User created",
        "toast": {"type": "success", "message": "x"},
    }


def test_reset_user_password_contract(client, mock_http_client):
    """Generated from POST /users/admin/reset-password in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"message": "Password reset", "dataWiped": True}
    )
    result = client.user_admin.reset_user_password(
        userId="x", username="x", newPassword="x", confirmDataWipe=True
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/admin/reset-password")
    assert sent.json == {
        "userId": "x",
        "username": "x",
        "newPassword": "x",
        "confirmDataWipe": True,
    }
    assert result.to_dict() == {"message": "Password reset", "dataWiped": True}


def test_disable_user_totp_contract(client, mock_http_client):
    """Generated from POST /users/admin/totp/disable in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"message": "TOTP disabled"})
    result = client.user_admin.disable_user_totp(userId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/admin/totp/disable")
    assert sent.json == {"userId": "x"}
    assert result.to_dict() == {"message": "TOTP disabled"}


def test_list_contract(client, mock_http_client):
    """Generated from GET /users/list in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "users": [
                {
                    "userId": "x",
                    "username": "x",
                    "is_admin": True,
                    "is_oidc": True,
                    "password_hash": "x",
                    "data_unlocked": True,
                    "totp_enabled": True,
                }
            ],
            "total": 1.0,
            "limit": 1.0,
            "offset": 1.0,
        },
    )
    result = client.user_admin.list()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/list")
    assert result.to_dict() == {
        "users": [
            {
                "userId": "x",
                "username": "x",
                "is_admin": True,
                "is_oidc": True,
                "password_hash": "x",
                "data_unlocked": True,
                "totp_enabled": True,
            }
        ],
        "total": 1.0,
        "limit": 1.0,
        "offset": 1.0,
    }


def test_export_user_contract(client, mock_http_client):
    """Generated from GET /users/admin/export/{userId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "version": "x",
            "exportedAt": "x",
            "userId": "x",
            "username": "x",
            "userData": {
                "sshHosts": ["x"],
                "sshCredentials": ["x"],
                "fileManagerData": {},
                "dismissedAlerts": ["x"],
            },
            "metadata": {"totalRecords": 1.0, "encrypted": True, "exportType": "all"},
        },
    )
    result = client.user_admin.export_user("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/admin/export/x")
    assert result.to_dict() == {
        "version": "x",
        "exportedAt": "x",
        "userId": "x",
        "username": "x",
        "userData": {
            "sshHosts": ["x"],
            "sshCredentials": ["x"],
            "fileManagerData": {},
            "dismissedAlerts": ["x"],
        },
        "metadata": {"totalRecords": 1.0, "encrypted": True, "exportType": "all"},
    }


def test_count_contract(client, mock_http_client):
    """Generated from GET /users/count in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"count": 1.0})
    result = client.user_admin.count()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/count")
    assert result.to_dict() == {"count": 1.0}


def test_get_db_health_contract(client, mock_http_client):
    """Generated from GET /users/db-health in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"status": "ok"})
    result = client.user_admin.get_db_health()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/db-health")
    assert result.to_dict() == {"status": "ok"}


def test_delete_user_contract(client, mock_http_client):
    """Generated from DELETE /users/delete-user in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"message": "x"})
    result = client.user_admin.delete_user()
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/users/delete-user")
    assert result.to_dict() == {"message": "x"}


@pytest.mark.asyncio
async def test_async_make_admin_contract(async_client, mock_async_http_client):
    """Generated from POST /users/make-admin in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"message": "x"})
    result = await async_client.user_admin.make_admin(userId="x", username="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/make-admin")
    assert sent.json == {"userId": "x", "username": "x"}
    assert result.to_dict() == {"message": "x"}


@pytest.mark.asyncio
async def test_async_remove_admin_contract(async_client, mock_async_http_client):
    """Generated from POST /users/remove-admin in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"message": "x"})
    result = await async_client.user_admin.remove_admin(userId="x", username="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/remove-admin")
    assert sent.json == {"userId": "x", "username": "x"}
    assert result.to_dict() == {"message": "x"}


@pytest.mark.asyncio
async def test_async_create_user_contract(async_client, mock_async_http_client):
    """Generated from POST /users/admin-create in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={"message": "User created", "toast": {"type": "success", "message": "x"}},
    )
    result = await async_client.user_admin.create_user(username="x", password="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/admin-create")
    assert sent.json == {"username": "x", "password": "x"}
    assert result.to_dict() == {
        "message": "User created",
        "toast": {"type": "success", "message": "x"},
    }


@pytest.mark.asyncio
async def test_async_reset_user_password_contract(async_client, mock_async_http_client):
    """Generated from POST /users/admin/reset-password in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"message": "Password reset", "dataWiped": True}
    )
    result = await async_client.user_admin.reset_user_password(
        userId="x", username="x", newPassword="x", confirmDataWipe=True
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/admin/reset-password")
    assert sent.json == {
        "userId": "x",
        "username": "x",
        "newPassword": "x",
        "confirmDataWipe": True,
    }
    assert result.to_dict() == {"message": "Password reset", "dataWiped": True}


@pytest.mark.asyncio
async def test_async_disable_user_totp_contract(async_client, mock_async_http_client):
    """Generated from POST /users/admin/totp/disable in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"message": "TOTP disabled"})
    result = await async_client.user_admin.disable_user_totp(userId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/admin/totp/disable")
    assert sent.json == {"userId": "x"}
    assert result.to_dict() == {"message": "TOTP disabled"}


@pytest.mark.asyncio
async def test_async_list_contract(async_client, mock_async_http_client):
    """Generated from GET /users/list in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "users": [
                {
                    "userId": "x",
                    "username": "x",
                    "is_admin": True,
                    "is_oidc": True,
                    "password_hash": "x",
                    "data_unlocked": True,
                    "totp_enabled": True,
                }
            ],
            "total": 1.0,
            "limit": 1.0,
            "offset": 1.0,
        },
    )
    result = await async_client.user_admin.list()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/list")
    assert result.to_dict() == {
        "users": [
            {
                "userId": "x",
                "username": "x",
                "is_admin": True,
                "is_oidc": True,
                "password_hash": "x",
                "data_unlocked": True,
                "totp_enabled": True,
            }
        ],
        "total": 1.0,
        "limit": 1.0,
        "offset": 1.0,
    }


@pytest.mark.asyncio
async def test_async_export_user_contract(async_client, mock_async_http_client):
    """Generated from GET /users/admin/export/{userId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "version": "x",
            "exportedAt": "x",
            "userId": "x",
            "username": "x",
            "userData": {
                "sshHosts": ["x"],
                "sshCredentials": ["x"],
                "fileManagerData": {},
                "dismissedAlerts": ["x"],
            },
            "metadata": {"totalRecords": 1.0, "encrypted": True, "exportType": "all"},
        },
    )
    result = await async_client.user_admin.export_user("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/admin/export/x")
    assert result.to_dict() == {
        "version": "x",
        "exportedAt": "x",
        "userId": "x",
        "username": "x",
        "userData": {
            "sshHosts": ["x"],
            "sshCredentials": ["x"],
            "fileManagerData": {},
            "dismissedAlerts": ["x"],
        },
        "metadata": {"totalRecords": 1.0, "encrypted": True, "exportType": "all"},
    }


@pytest.mark.asyncio
async def test_async_count_contract(async_client, mock_async_http_client):
    """Generated from GET /users/count in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"count": 1.0})
    result = await async_client.user_admin.count()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/count")
    assert result.to_dict() == {"count": 1.0}


@pytest.mark.asyncio
async def test_async_get_db_health_contract(async_client, mock_async_http_client):
    """Generated from GET /users/db-health in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"status": "ok"})
    result = await async_client.user_admin.get_db_health()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/db-health")
    assert result.to_dict() == {"status": "ok"}


@pytest.mark.asyncio
async def test_async_delete_user_contract(async_client, mock_async_http_client):
    """Generated from DELETE /users/delete-user in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"message": "x"})
    result = await async_client.user_admin.delete_user()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/users/delete-user")
    assert result.to_dict() == {"message": "x"}
