"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.collab."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_list_rooms_contract(client, mock_http_client):
    """Generated from GET /collab/rooms in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"rooms": ["x"]})
    result = client.collab.list_rooms()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/rooms")
    assert result.to_dict() == {"rooms": ["x"]}


def test_create_room_contract(client, mock_http_client):
    """Generated from POST /collab/rooms in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"room": {"guestLinkEnabled": True}})
    result = client.collab.create_room(name="x", persistent=True)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms")
    assert sent.json == {"name": "x", "persistent": True}
    assert result.to_dict() == {"room": {"guestLinkEnabled": True}}


def test_get_room_contract(client, mock_http_client):
    """Generated from GET /collab/rooms/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "room": {"guestLinkEnabled": True},
            "me": "x",
            "isHost": True,
            "members": [{"userId": "x", "username": "x", "roomRole": "x", "createdAt": "x"}],
            "online": [{"userId": "x", "username": "x"}],
            "stage": {"presenterUserId": "x", "protocol": "x", "hostId": 1.0, "shareId": "x"},
            "controllerUserId": "x",
            "controlRequests": [{"userId": "x", "username": "x", "requestedAt": "x"}],
        },
    )
    result = client.collab.get_room("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/rooms/x")
    assert result.to_dict() == {
        "room": {"guestLinkEnabled": True},
        "me": "x",
        "isHost": True,
        "members": [{"userId": "x", "username": "x", "roomRole": "x", "createdAt": "x"}],
        "online": [{"userId": "x", "username": "x"}],
        "stage": {"presenterUserId": "x", "protocol": "x", "hostId": 1.0, "shareId": "x"},
        "controllerUserId": "x",
        "controlRequests": [{"userId": "x", "username": "x", "requestedAt": "x"}],
    }


def test_delete_room_contract(client, mock_http_client):
    """Generated from DELETE /collab/rooms/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.collab.delete_room("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/collab/rooms/x")
    assert result.to_dict() == {"success": True}


def test_end_room_contract(client, mock_http_client):
    """Generated from POST /collab/rooms/{id}/end in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.collab.end_room("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/end")
    assert result.to_dict() == {"success": True}


def test_invite_members_contract(client, mock_http_client):
    """Generated from POST /collab/rooms/{id}/members in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.collab.invite_members("x", userIds=[], roleIds=[])
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/members")
    assert sent.json == {"userIds": [], "roleIds": []}
    assert result.to_dict() == {"success": True}


def test_remove_member_contract(client, mock_http_client):
    """Generated from DELETE /collab/rooms/{id}/members/{userId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.collab.remove_member("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/collab/rooms/x/members/x")
    assert result.to_dict() == {"success": True}


def test_present_contract(client, mock_http_client):
    """Generated from POST /collab/rooms/{id}/present in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={"stage": {"presenterUserId": "x", "protocol": "x", "hostId": 1.0, "shareId": "x"}},
    )
    result = client.collab.present("x", protocol="x", sessionId="x", hostId=1)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/present")
    assert sent.json == {"protocol": "x", "sessionId": "x", "hostId": 1}
    assert result.to_dict() == {
        "stage": {"presenterUserId": "x", "protocol": "x", "hostId": 1.0, "shareId": "x"}
    }


def test_stop_presenting_contract(client, mock_http_client):
    """Generated from POST /collab/rooms/{id}/stop in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.collab.stop_presenting("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/stop")
    assert result.to_dict() == {"success": True}


def test_get_stage_contract(client, mock_http_client):
    """Generated from GET /collab/rooms/{id}/stage in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"stage": "x"})
    result = client.collab.get_stage("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/rooms/x/stage")
    assert result.to_dict() == {"stage": "x"}


def test_set_control_contract(client, mock_http_client):
    """Generated from POST /collab/rooms/{id}/control in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"controllerUserId": "x"})
    result = client.collab.set_control("x", userId="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/control")
    assert sent.json == {"userId": "x"}
    assert result.to_dict() == {"controllerUserId": "x"}


def test_request_control_contract(client, mock_http_client):
    """Generated from POST /collab/rooms/{id}/control/request in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"request": {"userId": "x", "username": "x", "requestedAt": "x"}}
    )
    result = client.collab.request_control("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/control/request")
    assert result.to_dict() == {"request": {"userId": "x", "username": "x", "requestedAt": "x"}}


def test_list_control_requests_contract(client, mock_http_client):
    """Generated from GET /collab/rooms/{id}/control/requests in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"requests": [{"userId": "x", "username": "x", "requestedAt": "x"}]}
    )
    result = client.collab.list_control_requests("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/rooms/x/control/requests")
    assert result.to_dict() == {"requests": [{"userId": "x", "username": "x", "requestedAt": "x"}]}


def test_delete_control_request_contract(client, mock_http_client):
    """Generated from DELETE /collab/rooms/{id}/control/requests/{userId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.collab.delete_control_request("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/collab/rooms/x/control/requests/x")
    assert result.to_dict() == {"success": True}


def test_set_guest_link_contract(client, mock_http_client):
    """Generated from POST /collab/rooms/{id}/guest-link in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"guestLinkToken": "x"})
    result = client.collab.set_guest_link("x", enabled=True)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/guest-link")
    assert sent.json == {"enabled": True}
    assert result.to_dict() == {"guestLinkToken": "x"}


def test_get_guest_stage_contract(client, mock_http_client):
    """Generated from GET /collab/guest/{token} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"roomName": "x", "stage": "x"})
    result = client.collab.get_guest_stage("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/guest/x")
    assert result.to_dict() == {"roomName": "x", "stage": "x"}


@pytest.mark.asyncio
async def test_async_list_rooms_contract(async_client, mock_async_http_client):
    """Generated from GET /collab/rooms in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"rooms": ["x"]})
    result = await async_client.collab.list_rooms()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/rooms")
    assert result.to_dict() == {"rooms": ["x"]}


@pytest.mark.asyncio
async def test_async_create_room_contract(async_client, mock_async_http_client):
    """Generated from POST /collab/rooms in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"room": {"guestLinkEnabled": True}}
    )
    result = await async_client.collab.create_room(name="x", persistent=True)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms")
    assert sent.json == {"name": "x", "persistent": True}
    assert result.to_dict() == {"room": {"guestLinkEnabled": True}}


@pytest.mark.asyncio
async def test_async_get_room_contract(async_client, mock_async_http_client):
    """Generated from GET /collab/rooms/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "room": {"guestLinkEnabled": True},
            "me": "x",
            "isHost": True,
            "members": [{"userId": "x", "username": "x", "roomRole": "x", "createdAt": "x"}],
            "online": [{"userId": "x", "username": "x"}],
            "stage": {"presenterUserId": "x", "protocol": "x", "hostId": 1.0, "shareId": "x"},
            "controllerUserId": "x",
            "controlRequests": [{"userId": "x", "username": "x", "requestedAt": "x"}],
        },
    )
    result = await async_client.collab.get_room("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/rooms/x")
    assert result.to_dict() == {
        "room": {"guestLinkEnabled": True},
        "me": "x",
        "isHost": True,
        "members": [{"userId": "x", "username": "x", "roomRole": "x", "createdAt": "x"}],
        "online": [{"userId": "x", "username": "x"}],
        "stage": {"presenterUserId": "x", "protocol": "x", "hostId": 1.0, "shareId": "x"},
        "controllerUserId": "x",
        "controlRequests": [{"userId": "x", "username": "x", "requestedAt": "x"}],
    }


@pytest.mark.asyncio
async def test_async_delete_room_contract(async_client, mock_async_http_client):
    """Generated from DELETE /collab/rooms/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.collab.delete_room("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/collab/rooms/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_end_room_contract(async_client, mock_async_http_client):
    """Generated from POST /collab/rooms/{id}/end in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.collab.end_room("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/end")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_invite_members_contract(async_client, mock_async_http_client):
    """Generated from POST /collab/rooms/{id}/members in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.collab.invite_members("x", userIds=[], roleIds=[])
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/members")
    assert sent.json == {"userIds": [], "roleIds": []}
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_remove_member_contract(async_client, mock_async_http_client):
    """Generated from DELETE /collab/rooms/{id}/members/{userId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.collab.remove_member("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/collab/rooms/x/members/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_present_contract(async_client, mock_async_http_client):
    """Generated from POST /collab/rooms/{id}/present in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={"stage": {"presenterUserId": "x", "protocol": "x", "hostId": 1.0, "shareId": "x"}},
    )
    result = await async_client.collab.present("x", protocol="x", sessionId="x", hostId=1)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/present")
    assert sent.json == {"protocol": "x", "sessionId": "x", "hostId": 1}
    assert result.to_dict() == {
        "stage": {"presenterUserId": "x", "protocol": "x", "hostId": 1.0, "shareId": "x"}
    }


@pytest.mark.asyncio
async def test_async_stop_presenting_contract(async_client, mock_async_http_client):
    """Generated from POST /collab/rooms/{id}/stop in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.collab.stop_presenting("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/stop")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_get_stage_contract(async_client, mock_async_http_client):
    """Generated from GET /collab/rooms/{id}/stage in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"stage": "x"})
    result = await async_client.collab.get_stage("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/rooms/x/stage")
    assert result.to_dict() == {"stage": "x"}


@pytest.mark.asyncio
async def test_async_set_control_contract(async_client, mock_async_http_client):
    """Generated from POST /collab/rooms/{id}/control in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"controllerUserId": "x"})
    result = await async_client.collab.set_control("x", userId="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/control")
    assert sent.json == {"userId": "x"}
    assert result.to_dict() == {"controllerUserId": "x"}


@pytest.mark.asyncio
async def test_async_request_control_contract(async_client, mock_async_http_client):
    """Generated from POST /collab/rooms/{id}/control/request in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"request": {"userId": "x", "username": "x", "requestedAt": "x"}}
    )
    result = await async_client.collab.request_control("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/control/request")
    assert result.to_dict() == {"request": {"userId": "x", "username": "x", "requestedAt": "x"}}


@pytest.mark.asyncio
async def test_async_list_control_requests_contract(async_client, mock_async_http_client):
    """Generated from GET /collab/rooms/{id}/control/requests in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"requests": [{"userId": "x", "username": "x", "requestedAt": "x"}]}
    )
    result = await async_client.collab.list_control_requests("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/rooms/x/control/requests")
    assert result.to_dict() == {"requests": [{"userId": "x", "username": "x", "requestedAt": "x"}]}


@pytest.mark.asyncio
async def test_async_delete_control_request_contract(async_client, mock_async_http_client):
    """Generated from DELETE /collab/rooms/{id}/control/requests/{userId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.collab.delete_control_request("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/collab/rooms/x/control/requests/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_set_guest_link_contract(async_client, mock_async_http_client):
    """Generated from POST /collab/rooms/{id}/guest-link in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"guestLinkToken": "x"})
    result = await async_client.collab.set_guest_link("x", enabled=True)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/collab/rooms/x/guest-link")
    assert sent.json == {"enabled": True}
    assert result.to_dict() == {"guestLinkToken": "x"}


@pytest.mark.asyncio
async def test_async_get_guest_stage_contract(async_client, mock_async_http_client):
    """Generated from GET /collab/guest/{token} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"roomName": "x", "stage": "x"})
    result = await async_client.collab.get_guest_stage("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/collab/guest/x")
    assert result.to_dict() == {"roomName": "x", "stage": "x"}
