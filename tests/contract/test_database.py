"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.database."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_import_data_contract(client, mock_http_client):
    """Generated from POST /database/import in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "message": "Incremental import completed successfully",
            "summary": {
                "sshHostsImported": 1.0,
                "sshCredentialsImported": 1.0,
                "fileManagerItemsImported": 1.0,
                "dismissedAlertsImported": 1.0,
                "credentialUsageImported": 1.0,
                "settingsImported": 1.0,
                "skippedItems": 1.0,
                "errors": ["x"],
            },
        },
    )
    result = client.database.import_data(file=b"file-content-placeholder")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/database/import")
    assert sent.files == {"file": b"file-content-placeholder"}
    assert result.to_dict() == {
        "success": True,
        "message": "Incremental import completed successfully",
        "summary": {
            "sshHostsImported": 1.0,
            "sshCredentialsImported": 1.0,
            "fileManagerItemsImported": 1.0,
            "dismissedAlertsImported": 1.0,
            "credentialUsageImported": 1.0,
            "settingsImported": 1.0,
            "skippedItems": 1.0,
            "errors": ["x"],
        },
    }


def test_preview_export_contract(client, mock_http_client):
    """Generated from POST /database/export/preview in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "preview": True,
            "stats": {
                "version": "x",
                "exportedAt": "x",
                "username": "x",
                "totalRecords": 1.0,
                "breakdown": {
                    "sshHosts": 1.0,
                    "sshCredentials": 1.0,
                    "fileManagerItems": 1.0,
                    "dismissedAlerts": 1.0,
                },
                "encrypted": True,
            },
            "estimatedSize": 1.0,
        },
    )
    result = client.database.preview_export(scope="x", includeCredentials=True)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/database/export/preview")
    assert sent.json == {"scope": "x", "includeCredentials": True}
    assert result.to_dict() == {
        "preview": True,
        "stats": {
            "version": "x",
            "exportedAt": "x",
            "username": "x",
            "totalRecords": 1.0,
            "breakdown": {
                "sshHosts": 1.0,
                "sshCredentials": 1.0,
                "fileManagerItems": 1.0,
                "dismissedAlerts": 1.0,
            },
            "encrypted": True,
        },
        "estimatedSize": 1.0,
    }


def test_restore_contract(client, mock_http_client):
    """Generated from POST /database/restore in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={"success": True, "message": "Database restored successfully", "restoredPath": "x"},
    )
    result = client.database.restore(backupPath="x", targetPath="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/database/restore")
    assert sent.json == {"backupPath": "x", "targetPath": "x"}
    assert result.to_dict() == {
        "success": True,
        "message": "Database restored successfully",
        "restoredPath": "x",
    }


def test_migration_status_contract(client, mock_http_client):
    """Generated from GET /database/migration/status in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "migrationStatus": {
                "needsMigration": True,
                "hasUnencryptedDb": True,
                "hasEncryptedDb": True,
                "unencryptedDbSize": 1.0,
                "reason": "x",
            },
            "files": {
                "unencryptedDbSize": 0,
                "encryptedDbSize": 0,
                "backupFiles": 1.0,
                "migratedFiles": 1.0,
            },
        },
    )
    result = client.database.migration_status()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/database/migration/status")
    assert result.to_dict() == {
        "migrationStatus": {
            "needsMigration": True,
            "hasUnencryptedDb": True,
            "hasEncryptedDb": True,
            "unencryptedDbSize": 1.0,
            "reason": "x",
        },
        "files": {
            "unencryptedDbSize": 0,
            "encryptedDbSize": 0,
            "backupFiles": 1.0,
            "migratedFiles": 1.0,
        },
    }


def test_migration_history_contract(client, mock_http_client):
    """Generated from GET /database/migration/history in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "files": ["x"],
            "summary": {
                "totalBackups": 1.0,
                "totalMigrated": 1.0,
                "oldestBackup": {},
                "newestBackup": {},
            },
        },
    )
    result = client.database.migration_history()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/database/migration/history")
    assert result.to_dict() == {
        "files": ["x"],
        "summary": {
            "totalBackups": 1.0,
            "totalMigrated": 1.0,
            "oldestBackup": {},
            "newestBackup": {},
        },
    }


def test_export_contract(client, mock_http_client):
    """Generated from POST /database/export in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"x-contract-test-placeholder": True})
    result = client.database.export()
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/database/export")
    assert result.to_dict() == {"x-contract-test-placeholder": True}


@pytest.mark.asyncio
async def test_async_import_data_contract(async_client, mock_async_http_client):
    """Generated from POST /database/import in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "message": "Incremental import completed successfully",
            "summary": {
                "sshHostsImported": 1.0,
                "sshCredentialsImported": 1.0,
                "fileManagerItemsImported": 1.0,
                "dismissedAlertsImported": 1.0,
                "credentialUsageImported": 1.0,
                "settingsImported": 1.0,
                "skippedItems": 1.0,
                "errors": ["x"],
            },
        },
    )
    result = await async_client.database.import_data(file=b"file-content-placeholder")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/database/import")
    assert sent.files == {"file": b"file-content-placeholder"}
    assert result.to_dict() == {
        "success": True,
        "message": "Incremental import completed successfully",
        "summary": {
            "sshHostsImported": 1.0,
            "sshCredentialsImported": 1.0,
            "fileManagerItemsImported": 1.0,
            "dismissedAlertsImported": 1.0,
            "credentialUsageImported": 1.0,
            "settingsImported": 1.0,
            "skippedItems": 1.0,
            "errors": ["x"],
        },
    }


@pytest.mark.asyncio
async def test_async_preview_export_contract(async_client, mock_async_http_client):
    """Generated from POST /database/export/preview in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "preview": True,
            "stats": {
                "version": "x",
                "exportedAt": "x",
                "username": "x",
                "totalRecords": 1.0,
                "breakdown": {
                    "sshHosts": 1.0,
                    "sshCredentials": 1.0,
                    "fileManagerItems": 1.0,
                    "dismissedAlerts": 1.0,
                },
                "encrypted": True,
            },
            "estimatedSize": 1.0,
        },
    )
    result = await async_client.database.preview_export(scope="x", includeCredentials=True)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/database/export/preview")
    assert sent.json == {"scope": "x", "includeCredentials": True}
    assert result.to_dict() == {
        "preview": True,
        "stats": {
            "version": "x",
            "exportedAt": "x",
            "username": "x",
            "totalRecords": 1.0,
            "breakdown": {
                "sshHosts": 1.0,
                "sshCredentials": 1.0,
                "fileManagerItems": 1.0,
                "dismissedAlerts": 1.0,
            },
            "encrypted": True,
        },
        "estimatedSize": 1.0,
    }


@pytest.mark.asyncio
async def test_async_restore_contract(async_client, mock_async_http_client):
    """Generated from POST /database/restore in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={"success": True, "message": "Database restored successfully", "restoredPath": "x"},
    )
    result = await async_client.database.restore(backupPath="x", targetPath="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/database/restore")
    assert sent.json == {"backupPath": "x", "targetPath": "x"}
    assert result.to_dict() == {
        "success": True,
        "message": "Database restored successfully",
        "restoredPath": "x",
    }


@pytest.mark.asyncio
async def test_async_migration_status_contract(async_client, mock_async_http_client):
    """Generated from GET /database/migration/status in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "migrationStatus": {
                "needsMigration": True,
                "hasUnencryptedDb": True,
                "hasEncryptedDb": True,
                "unencryptedDbSize": 1.0,
                "reason": "x",
            },
            "files": {
                "unencryptedDbSize": 0,
                "encryptedDbSize": 0,
                "backupFiles": 1.0,
                "migratedFiles": 1.0,
            },
        },
    )
    result = await async_client.database.migration_status()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/database/migration/status")
    assert result.to_dict() == {
        "migrationStatus": {
            "needsMigration": True,
            "hasUnencryptedDb": True,
            "hasEncryptedDb": True,
            "unencryptedDbSize": 1.0,
            "reason": "x",
        },
        "files": {
            "unencryptedDbSize": 0,
            "encryptedDbSize": 0,
            "backupFiles": 1.0,
            "migratedFiles": 1.0,
        },
    }


@pytest.mark.asyncio
async def test_async_migration_history_contract(async_client, mock_async_http_client):
    """Generated from GET /database/migration/history in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "files": ["x"],
            "summary": {
                "totalBackups": 1.0,
                "totalMigrated": 1.0,
                "oldestBackup": {},
                "newestBackup": {},
            },
        },
    )
    result = await async_client.database.migration_history()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/database/migration/history")
    assert result.to_dict() == {
        "files": ["x"],
        "summary": {
            "totalBackups": 1.0,
            "totalMigrated": 1.0,
            "oldestBackup": {},
            "newestBackup": {},
        },
    }


@pytest.mark.asyncio
async def test_async_export_contract(async_client, mock_async_http_client):
    """Generated from POST /database/export in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"x-contract-test-placeholder": True}
    )
    result = await async_client.database.export()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/database/export")
    assert result.to_dict() == {"x-contract-test-placeholder": True}
