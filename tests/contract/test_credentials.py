"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.credentials."""

from __future__ import annotations

import pytest


def test_list_contract(client, mock_http_client):
    """Generated from GET /credentials in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=[{}])
    result = client.credentials.list()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/credentials")
    assert result == [{}]


def test_create_contract(client, mock_http_client):
    """Generated from POST /credentials in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.credentials.create(
        name="x",
        description="x",
        folder="x",
        tags=[],
        authType="password",
        username="x",
        password="x",
        key="x",
        keyPassword="x",
        keyType="x",
        certPublicKey="x",
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials")
    assert sent.json == {
        "name": "x",
        "description": "x",
        "folder": "x",
        "tags": [],
        "authType": "password",
        "username": "x",
        "password": "x",
        "key": "x",
        "keyPassword": "x",
        "keyType": "x",
        "certPublicKey": "x",
    }
    assert result is not None


def test_list_folders_contract(client, mock_http_client):
    """Generated from GET /credentials/folders in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=["x"])
    result = client.credentials.list_folders()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/credentials/folders")
    assert result == ["x"]


def test_rename_folder_contract(client, mock_http_client):
    """Generated from PUT /credentials/folders/rename in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Folder renamed successfully"}
    )
    result = client.credentials.rename_folder(oldName="x", newName="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/credentials/folders/rename")
    assert sent.json == {"oldName": "x", "newName": "x"}
    assert result.to_dict() == {"success": True, "message": "Folder renamed successfully"}


def test_reorder_contract(client, mock_http_client):
    """Generated from PUT /credentials/reorder in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"updated": 1.0})
    result = client.credentials.reorder(positions=[])
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/credentials/reorder")
    assert sent.json == {"positions": []}
    assert result.to_dict() == {"updated": 1.0}


def test_generate_key_pair_contract(client, mock_http_client):
    """Generated from POST /credentials/generate-key-pair in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "privateKey": "x",
            "publicKey": "x",
            "keyType": "x",
            "format": "ssh",
            "algorithm": "x",
            "keySize": "x",
            "curve": "x",
        },
    )
    result = client.credentials.generate_key_pair(keyType="ssh-rsa", keySize=1.0, passphrase="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/generate-key-pair")
    assert sent.json == {"keyType": "ssh-rsa", "keySize": 1.0, "passphrase": "x"}
    assert result.to_dict() == {
        "success": True,
        "privateKey": "x",
        "publicKey": "x",
        "keyType": "x",
        "format": "ssh",
        "algorithm": "x",
        "keySize": "x",
        "curve": "x",
    }


def test_generate_public_key_contract(client, mock_http_client):
    """Generated from POST /credentials/generate-public-key in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"success": True, "publicKey": "x", "keyType": "x"}
    )
    result = client.credentials.generate_public_key(privateKey="x", keyPassword="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/generate-public-key")
    assert sent.json == {"privateKey": "x", "keyPassword": "x"}
    assert result.to_dict() == {"success": True, "publicKey": "x", "keyType": "x"}


def test_detect_key_type_contract(client, mock_http_client):
    """Generated from POST /credentials/detect-key-type in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "keyType": "x",
            "detectedKeyType": "x",
            "hasPublicKey": True,
            "error": "x",
        },
    )
    result = client.credentials.detect_key_type(privateKey="x", keyPassword="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/detect-key-type")
    assert sent.json == {"privateKey": "x", "keyPassword": "x"}
    assert result.to_dict() == {
        "success": True,
        "keyType": "x",
        "detectedKeyType": "x",
        "hasPublicKey": True,
        "error": "x",
    }


def test_detect_public_key_type_contract(client, mock_http_client):
    """Generated from POST /credentials/detect-public-key-type in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={"success": True, "keyType": "x", "detectedKeyType": "x", "error": "x"},
    )
    result = client.credentials.detect_public_key_type(publicKey="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/detect-public-key-type")
    assert sent.json == {"publicKey": "x"}
    assert result.to_dict() == {
        "success": True,
        "keyType": "x",
        "detectedKeyType": "x",
        "error": "x",
    }


def test_validate_key_pair_contract(client, mock_http_client):
    """Generated from POST /credentials/validate-key-pair in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "isValid": True,
            "privateKeyType": "x",
            "publicKeyType": "x",
            "generatedPublicKey": "x",
            "error": "x",
        },
    )
    result = client.credentials.validate_key_pair(privateKey="x", publicKey="x", keyPassword="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/validate-key-pair")
    assert sent.json == {"privateKey": "x", "publicKey": "x", "keyPassword": "x"}
    assert result.to_dict() == {
        "isValid": True,
        "privateKeyType": "x",
        "publicKeyType": "x",
        "generatedPublicKey": "x",
        "error": "x",
    }


def test_retrieve_contract(client, mock_http_client):
    """Generated from GET /credentials/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.credentials.retrieve("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/credentials/x")
    assert result is not None


def test_update_contract(client, mock_http_client):
    """Generated from PUT /credentials/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.credentials.update(
        "x",
        name="x",
        description="x",
        folder="x",
        tags="x",
        username="x",
        authType="x",
        keyType="x",
        password="x",
        key="x",
        keyPassword="x",
        certPublicKey="x",
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/credentials/x")
    assert sent.json == {
        "name": "x",
        "description": "x",
        "folder": "x",
        "tags": "x",
        "username": "x",
        "authType": "x",
        "keyType": "x",
        "password": "x",
        "key": "x",
        "keyPassword": "x",
        "certPublicKey": "x",
    }
    assert result is not None


def test_delete_contract(client, mock_http_client):
    """Generated from DELETE /credentials/{id} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"message": "Credential deleted successfully"}
    )
    result = client.credentials.delete("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/credentials/x")
    assert result.to_dict() == {"message": "Credential deleted successfully"}


def test_apply_to_host_contract(client, mock_http_client):
    """Generated from POST /credentials/{id}/apply-to-host/{hostId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"message": "Credential applied to host successfully"}
    )
    result = client.credentials.apply_to_host("x", "x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/x/apply-to-host/x")
    assert result.to_dict() == {"message": "Credential applied to host successfully"}


def test_deploy_to_host_contract(client, mock_http_client):
    """Generated from POST /credentials/{id}/deploy-to-host in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True, "message": "x"})
    result = client.credentials.deploy_to_host("x", targetHostId=1.0)
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/x/deploy-to-host")
    assert sent.json == {"targetHostId": 1.0}
    assert result.to_dict() == {"success": True, "message": "x"}


def test_duplicate_contract(client, mock_http_client):
    """Generated from POST /credentials/{id}/duplicate in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={})
    result = client.credentials.duplicate(
        "x", name="x", username="x", password="x", key="x", keyPassword="x", certPublicKey="x"
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/x/duplicate")
    assert sent.json == {
        "name": "x",
        "username": "x",
        "password": "x",
        "key": "x",
        "keyPassword": "x",
        "certPublicKey": "x",
    }
    assert result is not None


def test_hosts_contract(client, mock_http_client):
    """Generated from GET /credentials/{id}/hosts in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body=["x"])
    result = client.credentials.hosts("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/credentials/x/hosts")
    assert result == ["x"]


@pytest.mark.asyncio
async def test_async_list_contract(async_client, mock_async_http_client):
    """Generated from GET /credentials in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=[{}])
    result = await async_client.credentials.list()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/credentials")
    assert result == [{}]


@pytest.mark.asyncio
async def test_async_create_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.credentials.create(
        name="x",
        description="x",
        folder="x",
        tags=[],
        authType="password",
        username="x",
        password="x",
        key="x",
        keyPassword="x",
        keyType="x",
        certPublicKey="x",
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials")
    assert sent.json == {
        "name": "x",
        "description": "x",
        "folder": "x",
        "tags": [],
        "authType": "password",
        "username": "x",
        "password": "x",
        "key": "x",
        "keyPassword": "x",
        "keyType": "x",
        "certPublicKey": "x",
    }
    assert result is not None


@pytest.mark.asyncio
async def test_async_list_folders_contract(async_client, mock_async_http_client):
    """Generated from GET /credentials/folders in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=["x"])
    result = await async_client.credentials.list_folders()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/credentials/folders")
    assert result == ["x"]


@pytest.mark.asyncio
async def test_async_rename_folder_contract(async_client, mock_async_http_client):
    """Generated from PUT /credentials/folders/rename in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "message": "Folder renamed successfully"}
    )
    result = await async_client.credentials.rename_folder(oldName="x", newName="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/credentials/folders/rename")
    assert sent.json == {"oldName": "x", "newName": "x"}
    assert result.to_dict() == {"success": True, "message": "Folder renamed successfully"}


@pytest.mark.asyncio
async def test_async_reorder_contract(async_client, mock_async_http_client):
    """Generated from PUT /credentials/reorder in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"updated": 1.0})
    result = await async_client.credentials.reorder(positions=[])
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/credentials/reorder")
    assert sent.json == {"positions": []}
    assert result.to_dict() == {"updated": 1.0}


@pytest.mark.asyncio
async def test_async_generate_key_pair_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials/generate-key-pair in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "privateKey": "x",
            "publicKey": "x",
            "keyType": "x",
            "format": "ssh",
            "algorithm": "x",
            "keySize": "x",
            "curve": "x",
        },
    )
    result = await async_client.credentials.generate_key_pair(
        keyType="ssh-rsa", keySize=1.0, passphrase="x"
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/generate-key-pair")
    assert sent.json == {"keyType": "ssh-rsa", "keySize": 1.0, "passphrase": "x"}
    assert result.to_dict() == {
        "success": True,
        "privateKey": "x",
        "publicKey": "x",
        "keyType": "x",
        "format": "ssh",
        "algorithm": "x",
        "keySize": "x",
        "curve": "x",
    }


@pytest.mark.asyncio
async def test_async_generate_public_key_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials/generate-public-key in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"success": True, "publicKey": "x", "keyType": "x"}
    )
    result = await async_client.credentials.generate_public_key(privateKey="x", keyPassword="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/generate-public-key")
    assert sent.json == {"privateKey": "x", "keyPassword": "x"}
    assert result.to_dict() == {"success": True, "publicKey": "x", "keyType": "x"}


@pytest.mark.asyncio
async def test_async_detect_key_type_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials/detect-key-type in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "success": True,
            "keyType": "x",
            "detectedKeyType": "x",
            "hasPublicKey": True,
            "error": "x",
        },
    )
    result = await async_client.credentials.detect_key_type(privateKey="x", keyPassword="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/detect-key-type")
    assert sent.json == {"privateKey": "x", "keyPassword": "x"}
    assert result.to_dict() == {
        "success": True,
        "keyType": "x",
        "detectedKeyType": "x",
        "hasPublicKey": True,
        "error": "x",
    }


@pytest.mark.asyncio
async def test_async_detect_public_key_type_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials/detect-public-key-type in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={"success": True, "keyType": "x", "detectedKeyType": "x", "error": "x"},
    )
    result = await async_client.credentials.detect_public_key_type(publicKey="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/detect-public-key-type")
    assert sent.json == {"publicKey": "x"}
    assert result.to_dict() == {
        "success": True,
        "keyType": "x",
        "detectedKeyType": "x",
        "error": "x",
    }


@pytest.mark.asyncio
async def test_async_validate_key_pair_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials/validate-key-pair in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "isValid": True,
            "privateKeyType": "x",
            "publicKeyType": "x",
            "generatedPublicKey": "x",
            "error": "x",
        },
    )
    result = await async_client.credentials.validate_key_pair(
        privateKey="x", publicKey="x", keyPassword="x"
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/validate-key-pair")
    assert sent.json == {"privateKey": "x", "publicKey": "x", "keyPassword": "x"}
    assert result.to_dict() == {
        "isValid": True,
        "privateKeyType": "x",
        "publicKeyType": "x",
        "generatedPublicKey": "x",
        "error": "x",
    }


@pytest.mark.asyncio
async def test_async_retrieve_contract(async_client, mock_async_http_client):
    """Generated from GET /credentials/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.credentials.retrieve("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/credentials/x")
    assert result is not None


@pytest.mark.asyncio
async def test_async_update_contract(async_client, mock_async_http_client):
    """Generated from PUT /credentials/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.credentials.update(
        "x",
        name="x",
        description="x",
        folder="x",
        tags="x",
        username="x",
        authType="x",
        keyType="x",
        password="x",
        key="x",
        keyPassword="x",
        certPublicKey="x",
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "PUT"
    assert sent.url.endswith("/credentials/x")
    assert sent.json == {
        "name": "x",
        "description": "x",
        "folder": "x",
        "tags": "x",
        "username": "x",
        "authType": "x",
        "keyType": "x",
        "password": "x",
        "key": "x",
        "keyPassword": "x",
        "certPublicKey": "x",
    }
    assert result is not None


@pytest.mark.asyncio
async def test_async_delete_contract(async_client, mock_async_http_client):
    """Generated from DELETE /credentials/{id} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"message": "Credential deleted successfully"}
    )
    result = await async_client.credentials.delete("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/credentials/x")
    assert result.to_dict() == {"message": "Credential deleted successfully"}


@pytest.mark.asyncio
async def test_async_apply_to_host_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials/{id}/apply-to-host/{hostId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"message": "Credential applied to host successfully"}
    )
    result = await async_client.credentials.apply_to_host("x", "x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/x/apply-to-host/x")
    assert result.to_dict() == {"message": "Credential applied to host successfully"}


@pytest.mark.asyncio
async def test_async_deploy_to_host_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials/{id}/deploy-to-host in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True, "message": "x"})
    result = await async_client.credentials.deploy_to_host("x", targetHostId=1.0)
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/x/deploy-to-host")
    assert sent.json == {"targetHostId": 1.0}
    assert result.to_dict() == {"success": True, "message": "x"}


@pytest.mark.asyncio
async def test_async_duplicate_contract(async_client, mock_async_http_client):
    """Generated from POST /credentials/{id}/duplicate in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={})
    result = await async_client.credentials.duplicate(
        "x", name="x", username="x", password="x", key="x", keyPassword="x", certPublicKey="x"
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/credentials/x/duplicate")
    assert sent.json == {
        "name": "x",
        "username": "x",
        "password": "x",
        "key": "x",
        "keyPassword": "x",
        "certPublicKey": "x",
    }
    assert result is not None


@pytest.mark.asyncio
async def test_async_hosts_contract(async_client, mock_async_http_client):
    """Generated from GET /credentials/{id}/hosts in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body=["x"])
    result = await async_client.credentials.hosts("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/credentials/x/hosts")
    assert result == ["x"]
