"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.session_sharing."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_create_contract(client, mock_http_client):
    """Generated from POST /session-sharing/create in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"shareId": "x", "linkToken": "x", "expiresAt": "x"}
    )
    result = client.session_sharing.create(
        hostId=1,
        sessionId="x",
        tabInstanceId="x",
        protocol="ssh",
        shareType="link",
        targetUserId="x",
        permissionLevel="read-only",
        expiryHours=1.0,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/session-sharing/create")
    assert sent.json == {
        "hostId": 1,
        "sessionId": "x",
        "tabInstanceId": "x",
        "protocol": "ssh",
        "shareType": "link",
        "targetUserId": "x",
        "permissionLevel": "read-only",
        "expiryHours": 1.0,
    }
    assert result.to_dict() == {"shareId": "x", "linkToken": "x", "expiresAt": "x"}


def test_get_active_for_host_contract(client, mock_http_client):
    """Generated from GET /session-sharing/host/{hostId}/active in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"shares": [{}]})
    result = client.session_sharing.get_active_for_host("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/session-sharing/host/x/active")
    assert result.to_dict() == {"shares": [{}]}


def test_delete_contract(client, mock_http_client):
    """Generated from DELETE /session-sharing/{shareId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.session_sharing.delete("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/session-sharing/x")
    assert result.to_dict() == {"success": True}


def test_resolve_contract(client, mock_http_client):
    """Generated from GET /session-sharing/resolve/{linkToken} in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200, body={"protocol": "ssh", "permissionLevel": "read-only", "wsPath": "x"}
    )
    result = client.session_sharing.resolve("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/session-sharing/resolve/x")
    assert result.to_dict() == {"protocol": "ssh", "permissionLevel": "read-only", "wsPath": "x"}


def test_end_contract(client, mock_http_client):
    """Generated from POST /session-sharing/{shareId}/end in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.session_sharing.end("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/session-sharing/x/end")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_create_contract(async_client, mock_async_http_client):
    """Generated from POST /session-sharing/create in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"shareId": "x", "linkToken": "x", "expiresAt": "x"}
    )
    result = await async_client.session_sharing.create(
        hostId=1,
        sessionId="x",
        tabInstanceId="x",
        protocol="ssh",
        shareType="link",
        targetUserId="x",
        permissionLevel="read-only",
        expiryHours=1.0,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/session-sharing/create")
    assert sent.json == {
        "hostId": 1,
        "sessionId": "x",
        "tabInstanceId": "x",
        "protocol": "ssh",
        "shareType": "link",
        "targetUserId": "x",
        "permissionLevel": "read-only",
        "expiryHours": 1.0,
    }
    assert result.to_dict() == {"shareId": "x", "linkToken": "x", "expiresAt": "x"}


@pytest.mark.asyncio
async def test_async_get_active_for_host_contract(async_client, mock_async_http_client):
    """Generated from GET /session-sharing/host/{hostId}/active in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"shares": [{}]})
    result = await async_client.session_sharing.get_active_for_host("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/session-sharing/host/x/active")
    assert result.to_dict() == {"shares": [{}]}


@pytest.mark.asyncio
async def test_async_delete_contract(async_client, mock_async_http_client):
    """Generated from DELETE /session-sharing/{shareId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.session_sharing.delete("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/session-sharing/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_resolve_contract(async_client, mock_async_http_client):
    """Generated from GET /session-sharing/resolve/{linkToken} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200, body={"protocol": "ssh", "permissionLevel": "read-only", "wsPath": "x"}
    )
    result = await async_client.session_sharing.resolve("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/session-sharing/resolve/x")
    assert result.to_dict() == {"protocol": "ssh", "permissionLevel": "read-only", "wsPath": "x"}


@pytest.mark.asyncio
async def test_async_end_contract(async_client, mock_async_http_client):
    """Generated from POST /session-sharing/{shareId}/end in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.session_sharing.end("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/session-sharing/x/end")
    assert result.to_dict() == {"success": True}
