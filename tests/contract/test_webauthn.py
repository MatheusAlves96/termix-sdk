"""Generated by tools/sdk-gen/generate.py. Do not edit by hand —
see docs/sdk-plan.md phases F3/F4. Covers termix_sdk.resources.webauthn."""

from __future__ import annotations

# ruff: noqa: E501
import pytest


def test_list_credentials_contract(client, mock_http_client):
    """Generated from GET /users/webauthn/credentials in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "credentials": [
                {
                    "id": "x",
                    "name": "x",
                    "deviceType": "x",
                    "backedUp": True,
                    "transports": ["ble"],
                    "userVerification": "x",
                    "createdAt": "x",
                    "lastUsedAt": "x",
                }
            ]
        },
    )
    result = client.webauthn.list_credentials()
    sent = mock_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/webauthn/credentials")
    assert result.to_dict() == {
        "credentials": [
            {
                "id": "x",
                "name": "x",
                "deviceType": "x",
                "backedUp": True,
                "transports": ["ble"],
                "userVerification": "x",
                "createdAt": "x",
                "lastUsedAt": "x",
            }
        ]
    }


def test_register_options_contract(client, mock_http_client):
    """Generated from POST /users/webauthn/register/options in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "options": {
                "rp": {"id": "x", "name": "x"},
                "user": {"id": "x", "name": "x", "displayName": "x"},
                "challenge": "x",
                "pubKeyCredParams": [{}],
                "timeout": 1.0,
                "excludeCredentials": [{}],
                "authenticatorSelection": {
                    "authenticatorAttachment": "platform",
                    "requireResidentKey": True,
                    "residentKey": "preferred",
                    "userVerification": "preferred",
                },
                "hints": ["hybrid"],
                "attestation": "none",
                "attestationFormats": ["none"],
                "extensions": {
                    "appid": "x",
                    "credProps": True,
                    "hmacCreateSecret": True,
                    "minPinLength": True,
                },
            },
            "challengeId": "x",
        },
    )
    result = client.webauthn.register_options(userVerification="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/webauthn/register/options")
    assert sent.json == {"userVerification": "x"}
    assert result.to_dict() == {
        "options": {
            "rp": {"id": "x", "name": "x"},
            "user": {"id": "x", "name": "x", "displayName": "x"},
            "challenge": "x",
            "pubKeyCredParams": [{}],
            "timeout": 1.0,
            "excludeCredentials": [{}],
            "authenticatorSelection": {
                "authenticatorAttachment": "platform",
                "requireResidentKey": True,
                "residentKey": "preferred",
                "userVerification": "preferred",
            },
            "hints": ["hybrid"],
            "attestation": "none",
            "attestationFormats": ["none"],
            "extensions": {
                "appid": "x",
                "credProps": True,
                "hmacCreateSecret": True,
                "minPinLength": True,
            },
        },
        "challengeId": "x",
    }


def test_register_verify_contract(client, mock_http_client):
    """Generated from POST /users/webauthn/register/verify in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.webauthn.register_verify(
        challengeId="x",
        response={
            "id": "x",
            "rawId": "x",
            "response": {
                "clientDataJSON": "x",
                "attestationObject": "x",
                "authenticatorData": "x",
                "transports": ["ble"],
                "publicKeyAlgorithm": 1.0,
                "publicKey": "x",
            },
            "authenticatorAttachment": "platform",
            "clientExtensionResults": {"appid": True, "credProps": {}, "hmacCreateSecret": True},
            "type": "x",
        },
        name="x",
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/webauthn/register/verify")
    assert sent.json == {
        "challengeId": "x",
        "response": {
            "id": "x",
            "rawId": "x",
            "response": {
                "clientDataJSON": "x",
                "attestationObject": "x",
                "authenticatorData": "x",
                "transports": ["ble"],
                "publicKeyAlgorithm": 1.0,
                "publicKey": "x",
            },
            "authenticatorAttachment": "platform",
            "clientExtensionResults": {"appid": True, "credProps": {}, "hmacCreateSecret": True},
            "type": "x",
        },
        "name": "x",
    }
    assert result.to_dict() == {"success": True}


def test_authenticate_options_contract(client, mock_http_client):
    """Generated from POST /users/webauthn/authenticate/options in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={
            "options": {
                "challenge": "x",
                "timeout": 1.0,
                "rpId": "x",
                "allowCredentials": [{}],
                "userVerification": "preferred",
                "hints": ["hybrid"],
                "extensions": {
                    "appid": "x",
                    "credProps": True,
                    "hmacCreateSecret": True,
                    "minPinLength": True,
                },
            },
            "challengeId": "x",
        },
    )
    result = client.webauthn.authenticate_options(userVerification="x", username="x")
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/webauthn/authenticate/options")
    assert sent.json == {"userVerification": "x", "username": "x"}
    assert result.to_dict() == {
        "options": {
            "challenge": "x",
            "timeout": 1.0,
            "rpId": "x",
            "allowCredentials": [{}],
            "userVerification": "preferred",
            "hints": ["hybrid"],
            "extensions": {
                "appid": "x",
                "credProps": True,
                "hmacCreateSecret": True,
                "minPinLength": True,
            },
        },
        "challengeId": "x",
    }


def test_authenticate_verify_contract(client, mock_http_client):
    """Generated from POST /users/webauthn/authenticate/verify in spec/termix-openapi.json."""
    mock_http_client.queue_response(
        status_code=200,
        body={"success": True, "requires_totp": True, "temp_token": "x", "rememberMe": True},
    )
    result = client.webauthn.authenticate_verify(
        challengeId="x",
        response={
            "id": "x",
            "rawId": "x",
            "response": {
                "clientDataJSON": "x",
                "authenticatorData": "x",
                "signature": "x",
                "userHandle": "x",
            },
            "authenticatorAttachment": "platform",
            "clientExtensionResults": {"appid": True, "credProps": {}, "hmacCreateSecret": True},
            "type": "x",
        },
        rememberMe=True,
    )
    sent = mock_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/webauthn/authenticate/verify")
    assert sent.json == {
        "challengeId": "x",
        "response": {
            "id": "x",
            "rawId": "x",
            "response": {
                "clientDataJSON": "x",
                "authenticatorData": "x",
                "signature": "x",
                "userHandle": "x",
            },
            "authenticatorAttachment": "platform",
            "clientExtensionResults": {"appid": True, "credProps": {}, "hmacCreateSecret": True},
            "type": "x",
        },
        "rememberMe": True,
    }
    assert result.to_dict() == {
        "success": True,
        "requires_totp": True,
        "temp_token": "x",
        "rememberMe": True,
    }


def test_delete_credential_contract(client, mock_http_client):
    """Generated from DELETE /users/webauthn/credentials/{credentialId} in spec/termix-openapi.json."""
    mock_http_client.queue_response(status_code=200, body={"success": True})
    result = client.webauthn.delete_credential("x")
    sent = mock_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/users/webauthn/credentials/x")
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_list_credentials_contract(async_client, mock_async_http_client):
    """Generated from GET /users/webauthn/credentials in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "credentials": [
                {
                    "id": "x",
                    "name": "x",
                    "deviceType": "x",
                    "backedUp": True,
                    "transports": ["ble"],
                    "userVerification": "x",
                    "createdAt": "x",
                    "lastUsedAt": "x",
                }
            ]
        },
    )
    result = await async_client.webauthn.list_credentials()
    sent = mock_async_http_client.requests[0]
    assert sent.method == "GET"
    assert sent.url.endswith("/users/webauthn/credentials")
    assert result.to_dict() == {
        "credentials": [
            {
                "id": "x",
                "name": "x",
                "deviceType": "x",
                "backedUp": True,
                "transports": ["ble"],
                "userVerification": "x",
                "createdAt": "x",
                "lastUsedAt": "x",
            }
        ]
    }


@pytest.mark.asyncio
async def test_async_register_options_contract(async_client, mock_async_http_client):
    """Generated from POST /users/webauthn/register/options in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "options": {
                "rp": {"id": "x", "name": "x"},
                "user": {"id": "x", "name": "x", "displayName": "x"},
                "challenge": "x",
                "pubKeyCredParams": [{}],
                "timeout": 1.0,
                "excludeCredentials": [{}],
                "authenticatorSelection": {
                    "authenticatorAttachment": "platform",
                    "requireResidentKey": True,
                    "residentKey": "preferred",
                    "userVerification": "preferred",
                },
                "hints": ["hybrid"],
                "attestation": "none",
                "attestationFormats": ["none"],
                "extensions": {
                    "appid": "x",
                    "credProps": True,
                    "hmacCreateSecret": True,
                    "minPinLength": True,
                },
            },
            "challengeId": "x",
        },
    )
    result = await async_client.webauthn.register_options(userVerification="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/webauthn/register/options")
    assert sent.json == {"userVerification": "x"}
    assert result.to_dict() == {
        "options": {
            "rp": {"id": "x", "name": "x"},
            "user": {"id": "x", "name": "x", "displayName": "x"},
            "challenge": "x",
            "pubKeyCredParams": [{}],
            "timeout": 1.0,
            "excludeCredentials": [{}],
            "authenticatorSelection": {
                "authenticatorAttachment": "platform",
                "requireResidentKey": True,
                "residentKey": "preferred",
                "userVerification": "preferred",
            },
            "hints": ["hybrid"],
            "attestation": "none",
            "attestationFormats": ["none"],
            "extensions": {
                "appid": "x",
                "credProps": True,
                "hmacCreateSecret": True,
                "minPinLength": True,
            },
        },
        "challengeId": "x",
    }


@pytest.mark.asyncio
async def test_async_register_verify_contract(async_client, mock_async_http_client):
    """Generated from POST /users/webauthn/register/verify in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.webauthn.register_verify(
        challengeId="x",
        response={
            "id": "x",
            "rawId": "x",
            "response": {
                "clientDataJSON": "x",
                "attestationObject": "x",
                "authenticatorData": "x",
                "transports": ["ble"],
                "publicKeyAlgorithm": 1.0,
                "publicKey": "x",
            },
            "authenticatorAttachment": "platform",
            "clientExtensionResults": {"appid": True, "credProps": {}, "hmacCreateSecret": True},
            "type": "x",
        },
        name="x",
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/webauthn/register/verify")
    assert sent.json == {
        "challengeId": "x",
        "response": {
            "id": "x",
            "rawId": "x",
            "response": {
                "clientDataJSON": "x",
                "attestationObject": "x",
                "authenticatorData": "x",
                "transports": ["ble"],
                "publicKeyAlgorithm": 1.0,
                "publicKey": "x",
            },
            "authenticatorAttachment": "platform",
            "clientExtensionResults": {"appid": True, "credProps": {}, "hmacCreateSecret": True},
            "type": "x",
        },
        "name": "x",
    }
    assert result.to_dict() == {"success": True}


@pytest.mark.asyncio
async def test_async_authenticate_options_contract(async_client, mock_async_http_client):
    """Generated from POST /users/webauthn/authenticate/options in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={
            "options": {
                "challenge": "x",
                "timeout": 1.0,
                "rpId": "x",
                "allowCredentials": [{}],
                "userVerification": "preferred",
                "hints": ["hybrid"],
                "extensions": {
                    "appid": "x",
                    "credProps": True,
                    "hmacCreateSecret": True,
                    "minPinLength": True,
                },
            },
            "challengeId": "x",
        },
    )
    result = await async_client.webauthn.authenticate_options(userVerification="x", username="x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/webauthn/authenticate/options")
    assert sent.json == {"userVerification": "x", "username": "x"}
    assert result.to_dict() == {
        "options": {
            "challenge": "x",
            "timeout": 1.0,
            "rpId": "x",
            "allowCredentials": [{}],
            "userVerification": "preferred",
            "hints": ["hybrid"],
            "extensions": {
                "appid": "x",
                "credProps": True,
                "hmacCreateSecret": True,
                "minPinLength": True,
            },
        },
        "challengeId": "x",
    }


@pytest.mark.asyncio
async def test_async_authenticate_verify_contract(async_client, mock_async_http_client):
    """Generated from POST /users/webauthn/authenticate/verify in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(
        status_code=200,
        body={"success": True, "requires_totp": True, "temp_token": "x", "rememberMe": True},
    )
    result = await async_client.webauthn.authenticate_verify(
        challengeId="x",
        response={
            "id": "x",
            "rawId": "x",
            "response": {
                "clientDataJSON": "x",
                "authenticatorData": "x",
                "signature": "x",
                "userHandle": "x",
            },
            "authenticatorAttachment": "platform",
            "clientExtensionResults": {"appid": True, "credProps": {}, "hmacCreateSecret": True},
            "type": "x",
        },
        rememberMe=True,
    )
    sent = mock_async_http_client.requests[0]
    assert sent.method == "POST"
    assert sent.url.endswith("/users/webauthn/authenticate/verify")
    assert sent.json == {
        "challengeId": "x",
        "response": {
            "id": "x",
            "rawId": "x",
            "response": {
                "clientDataJSON": "x",
                "authenticatorData": "x",
                "signature": "x",
                "userHandle": "x",
            },
            "authenticatorAttachment": "platform",
            "clientExtensionResults": {"appid": True, "credProps": {}, "hmacCreateSecret": True},
            "type": "x",
        },
        "rememberMe": True,
    }
    assert result.to_dict() == {
        "success": True,
        "requires_totp": True,
        "temp_token": "x",
        "rememberMe": True,
    }


@pytest.mark.asyncio
async def test_async_delete_credential_contract(async_client, mock_async_http_client):
    """Generated from DELETE /users/webauthn/credentials/{credentialId} in spec/termix-openapi.json."""
    mock_async_http_client.queue_response(status_code=200, body={"success": True})
    result = await async_client.webauthn.delete_credential("x")
    sent = mock_async_http_client.requests[0]
    assert sent.method == "DELETE"
    assert sent.url.endswith("/users/webauthn/credentials/x")
    assert result.to_dict() == {"success": True}
